---
status: active
item: L-260923-a9bdfe
---

# Plan — the skill size diet

The implementation tracker for [`design.md`](design.md), whose boxes were all ratified on 2026-09-23. The epic is `L-260923-a9bdfe`, which superseded the parked item `L-260921-7918a6`. Each phase below names its ledger item beside its heading once filed; the order between them is recorded as `blocked_by` on the later item. Every phase is a move in the sense of box G: it changes no behaviour beyond what box G lists, and its pull request names each exception.

## Handoff from the ratifying session — 2026-09-23

- **Done.** The design was written from a measurement at `fd26a2c` and every box ratified in an interview (box E amended, box I's order with it). The epic `L-260923-a9bdfe` superseded `L-260921-7918a6`, and the phase items below are filed under it with their order in `blocked_by`. Filed on the way: `L-260922-e01c73` (the inputs contradiction), `L-260922-2e3997` (the Python template gap, since carried by the `cli-python` epic `L-260923-71f4a9`), `L-260922-12f302` (the method-app family's initializer and serve target). `L-260923-7001f2`, which routes a Python CLI to `cli-python`, was noted with the ratified rulings and now waits on phase 5a.
- **The documents travel in PR #48** (`docs/Skill-size-diet-design`, worktree `_pipelex-plugins--skill-size-diet` at the workspace root). The design commits are `daa095a` (draft) and `832136b` (ratified, with this plan). Landing it takes a recorded `/rev` pass, then `/ledger-land`; the PR body says `Advances L-260923-a9bdfe`.
- **Next.** Louis starts phase 0 himself, in a new session: `wt add --for L-260923-75fb17`, then `ledger claim L-260923-75fb17 --renew` from inside it. If PR #48 has not landed, branch from `docs/Skill-size-diet-design` so the documents are there. `L-260922-e01c73` can land at any moment before phase 0.
- **Open questions.** None of Louis's are pending. What remains open is the facts list of phase 0 (design section 8), above all whether Codex and Vibe can locate and run a skill's `scripts/`, which box E rests on outside Claude.

## Standing rules for every phase

- **Classify before moving.** Each skill phase starts with a classification of the skill's text under box A — guard, branch, stop or rationale — written beside this plan as `classification-<skill>.md`, one row per paragraph with its destination. The pull request is reviewed against it, which is what makes a move readable as a move.
- **The shape is box B's**, and the rendered `SKILL.md` lands under the ceiling of box C on every target.
- **Rationale goes to `docs/decisions.md`** (box D), merged with what that file already says about the same rule.
- **Guards join the registry** (box H) in the same change that places them, and every pointer and reference is covered by the link check.
- **Smoke sessions close the phase** (box H): a fresh Claude Code session walks the main path and each branch, and one Codex session walks the main path. The plan records each scenario, the session id and whether the reference was read on its branch and not on the main path. Scenarios are written in the shape the parked eval suite (`L-260921-479a8e`) will take.
- **Open bugs against the skill wait for its phase** and land afterwards in the new shape; an urgent one lands whenever it is ready and the phase rebases onto it (box G).
- **Every phase ends with `/rev`**, and its release is an ordinary one.

## Before phase 0

- [ ] `L-260922-e01c73` lands: the three sentences of `pipelex-inputs` that still say preparation rewrites `inputs.json`, and the haiku example's directory. It is shipped and small, so it does not wait for the inputs phase (box G).

## Phase 0 — facts · `L-260923-75fb17`

The questions of design section 8, answered with evidence recorded here before any template moves.

- [x] On Codex: how the model learns a skill's directory; whether a relative link to `references/` resolves; whether a script under `scripts/` runs from the plugin's cache copy with its executable bit intact. Answered in a live session with a throwaway script in a local build. **Yes to all three** ([`facts.md`](facts.md) §3): the invoked skill carries its absolute path, a relative link resolves, and the script runs with its bit — with the skill's directory as its working directory.
- [x] The same three on Mistral Vibe. **Yes to all three once the skill is loaded** ([`facts.md`](facts.md) §4): Vibe states the base directory and runs skills in place; nothing is substituted in a body. Found on the way: Vibe drops `pipelex-design` and `pipelex-scaffold`, whose descriptions are not strict YAML — `L-260923-dfb8ee`, fixed on its own in #50 (`d3445f5`).
- [x] Whether Codex or Vibe carries an invoked skill across its own compaction, and within what budget. **Neither does, at any size**; both keep the path so the model can read the file again ([`facts.md`](facts.md) §3–4).
- [x] Whether `pipelex-integrate`'s instruction to copy `references/codegen-check.mjs` works on all three targets today. **On Claude and Codex, as a `cp`; on Vibe the model read and rewrote the file rather than copying it**, so the instruction must name a `cp` from the skill's directory (integrate's phase).
- [x] The token count of every rendered `SKILL.md` on every target, taken with the token-counting endpoint, which sets box C's character ceiling: 5,000 times the lowest characters-per-token ratio measured, less a stated margin. The proposed 16,000 holds only if no skill falls below 3.2 characters per token; density varies with code blocks and tables, so the smallest and the largest skill are not a sufficient sample. **Measured: 2.77 to 3.12 characters per token under the Claude 5 tokenizer, so the ceiling is 13,000 characters** ([`facts.md`](facts.md) §1). Claude Code itself measures the kept 5,000 tokens as characters divided by four, about 19,900 characters, so the ceiling holds under either reading.
- [x] The template variable that names a skill's directory on each target, settled from the answers above. **`skill_dir`**: `${CLAUDE_SKILL_DIR}` on Claude, a `<skill-dir>` placeholder defined in one sentence on Codex and Vibe; a script is run by its absolute path from the user's project, through its interpreter ([`facts.md`](facts.md) §5).

Phase 0 landed in #51 at `1e58d7e`.

## Phase 1 — the shared blocks and the machinery · `L-260923-8c296f`

- [x] The `mcp-requirements` include shrinks to its two stops — an absent tool, a `config`-class error surfaced verbatim — and a pointer to `shared/credentials.md`, a new shared reference rendered per target and read when the error is about the key (box F). The credential sentence corrected by `L-260912-65d6fc` moves there unchanged.
- [x] The deferrals the plugin-skills-gaps campaign assigned here (its `plan.md`, "Deferred"): one statement of the path form of `files`, replacing the sources in `pipelex-inputs` and `pipelex-integrate`; one include for the project markers design and integrate share; one wording of the stale-types notice; the `runs/` exclusion in the shared submission convention; the frontmatter's stray blank line.
- [x] The build copies `skills/<skill>/scripts/` beside `references/`, executable bits kept, and the freshness check covers both.
- [x] `scripts/check.py` gains the ceiling check in report mode (box C) and the link check both ways (box H), with unit tests.
- [x] The guard registry's machinery in the unit suite: a guard's canonical sentence asserted exactly once in each target's rendered `SKILL.md` and in no reference.
- [x] `CLAUDE.md` and `docs/build-targets.md` state the read-before-act rule and the shape, for whoever writes the next skill; `docs/decisions.md` records the campaign's decisions.
- [x] `/rev`: two rounds at profile 3 (cubic, Codex, code-review), each fixing what it confirmed — the link check's containment, anchors, fences and nested references, and the `runs/` exclusion carried into every gathering step. One duplication deferred below.

### Checkpoint 1

The machinery a skill phase relies on is on `dev`. Record the ceiling value, the skill-directory variable, the report the ceiling check prints, and any fact of phase 0 that changed a box.

- **The ceiling is 13,000 characters** (`SKILL_CEILING_CHARS` in `scripts/check.py`), in report mode (`SKILL_CEILING_ENFORCED = False`). At the end of phase 1 it reports every skill over it on every target, from `pipelex-scaffold` (about 63,700 characters on Claude) down to `pipelex-edit` (about 14,000). `pipelex-edit` is new to the list at this value, so phase 6 trims it with `pipelex-organize`.
- **The skill-directory variable is `skill_dir`, decided and not yet in `targets/`**: `${CLAUDE_SKILL_DIR}` on Claude, a `<skill-dir>` placeholder defined in one sentence on Codex and Vibe. It lands with the first skill that names a script or a verbatim copy — phase 2, for integrate's copy of its gate script, which Vibe performed as a read-and-rewrite until told to `cp` — because a variable nothing reads is a dead switch.
- **What phase 0 changed.** Box C's value (13,000, not 16,000). Box E holds on every target, with one addition to what a script owes the skill: it is run by its absolute path from the user's project, through its interpreter, because on Codex and Vibe the model otherwise runs it from inside the skill's directory. And one bug found on the way and fixed on its own, `L-260923-dfb8ee`: Vibe dropped two skills over their frontmatter.
- **What phase 1 changed beyond the move**, each named in its pull request: `design`, `edit`, `organize`, `explain` and `inputs` now exclude `runs/` from what they submit, through the shared convention (box F's deferral); the stale-types notice has one wording; the requirements block's recovery text moved to `shared/credentials.md`. Two items were filed from the work: `L-260923-58d533` (integrate's call site and gates load a run's `runs/` artifacts) and `L-260923-0964b0` (skills tell Codex users to type a Claude spelling of a skill).
- [x] `/rev` (the checkpoint's own review is phase 1's pass).

Phase 1 landed in #52 at `2fd7120`.

## Phase 2 — `pipelex-integrate` · `L-260923-5a94e7`

- [x] `classification-integrate.md`.
- [x] The main path: the three selectors, the rules each stated once, steps 1 to 12 as short imperatives, the target table, the sidecar's shape, the stop table. The narrowing of a list output through its `items` envelope stays as a guard.
- [x] References, each entered on its condition: the signature fallback when `main_pipe` is absent, refresh mode, the harness section, the report on orphans, the long gate-failure rows. What the returned results carry, and the narrowing code, go to `references/typescript.md` and `references/python.md`, which already hold the call-site templates.
- [x] The script candidates judged against box E's test: the containment check and the sidecar's hashes. Record the ruling and why.
- [x] Guards registered, smoke sessions run: a TypeScript project from a local bundle, a refresh, a project owning a harness, a run that meets orphans, and the Codex main path. **The guards are registered** (the `pipelex-integrate` entry of `GUARDS` in `tests/unit/test_skill_guards.py`), and **every smoke scenario passed**, recorded with its session ids in [`smoke-integrate.md`](smoke-integrate.md).
- [x] `/rev`. Two rounds at profile 4 (cubic, Codex review and adversarial, code-review). The first found that the rewrite had dropped qualifiers and remedies the classification never recorded dropping, and restored them: an interrupted run is this method's only when its artifacts are this target's, a valid but unrunnable method is finished in `/pipelex-design`, the launch directory itself passes containment, and a gate's exit `2` forbids regeneration only where the tree is not what failed. The second restored three more of the same kind — `main_pipe` "when it has one", the destination "by default", and the TypeScript bundle location — and cut one sentence of rationale `docs/decisions.md` already held to pay for them. What was deferred is under Deferred below.

- **Rendered sizes after the rewrite**: 12,948 characters on Claude, 12,806 on Codex and 12,852 on Vibe, after the review's fixes, all under the 13,000 ceiling. The Claude render was 60,753 characters before.
- **The references, each with its entry condition**, all indexed at the foot of `SKILL.md`: `refresh.md` (step 1 finds this method's `sources.json`, the user asks to refresh, or an editing skill hands off), `harness.md` (step 1 finds a `codegen` script or Makefile target, or a `sources.json` with a `derived` map), `signature-fallback.md` (step 3 finds no `main_pipe`), `orphans.md` (step 6 returns orphans and no drift), `gate-failures.md` (a gate exits `2`). `typescript.md` and `python.md` are read at step 1 by language, and now carry the list-output narrowing code and the account of what the results carry.
- **The script ruling: neither candidate became a script.** The containment check is two path resolutions and a comparison, and one of its inputs — the directory the session was launched in — is known to the model, not to a script run from the project; the tool refuses a climbing `output_dir` itself, and comparing the resolved paths answers the symlink case. The sidecar's hashes are verified by the gate step 10 installs and step 11 runs, which reports a wrong or missing hash, or a file left out, as `stale-source` before the report is written. Either becomes a candidate again if a smoke session shows a model getting it wrong. The full reasoning is in `docs/decisions.md`, under "`pipelex-integrate`'s rationale, moved out of the skill".
- **What the smoke sessions showed** ([`smoke-integrate.md`](smoke-integrate.md)): each branch reference was read on its branch and none on the main path, so no moved caveat was missed and no template changed. No session got either script candidate wrong: every sidecar's hashes passed the gate, and every session resolved the project root with `pwd -P` or `realpath` before writing. For checkpoint 2, these surfaced on the way. `typescript.md`'s known-defect note does not name a project that type-checks under `bundler` and runs its `tsc` output under plain Node, and that shape sent one Codex run into changing the project's build; the gap predates the diet, so by box G it lands after the phase. And an unattended Codex run must pre-approve `mthds_codegen` in its config, which the eval suite `L-260921-479a8e` will need.
- **The behaviour changes** are listed at the top of [`classification-integrate.md`](classification-integrate.md): the gate copy as a literal `cp` from the skill's directory, refresh mode deferring to step 4's rule on a lock without a sidecar, and the rule for reading a `codegen` script applied at step 1, where the harness branch is taken.

Waiting on this phase, to land afterwards in the new shape: `L-260913-3784bb`, `L-260913-48195f`, `L-260913-10e237`, `L-260913-7436ee`, `L-260913-0f8a89` (the orphan logic); `L-260912-dc69dc`, `L-260912-a4471f`, `L-260912-248381`, `L-260912-ac493c`, `L-260913-6cac17`, `L-260913-50b58c`, `L-260920-140727`, `L-260920-32b840`, `L-260922-65260d`, `L-260916-89e5b8`.

### Checkpoint 2

The first skill in the new shape. Before inputs and design repeat it, read the result against the design: whether the classification held, whether any moved caveat was missed in a smoke session, and whether box B's shape needs a correction. Record what changed.

- **Landed** in `pipelex-plugins#53` as `ee7abfe`, on `dev`.
- **The classification held in its placements, and leaked in its qualifiers.** Every sentence went where its row said, and no smoke session met a caveat that had moved out of its reach. But the rewrite shortened sentences as it placed them, and a shortened guard lost conditions its row never recorded dropping: the interrupted run's "the tree's artifacts are the target's", the signature's "when the verdict carries it", the destination's "by default", the launch directory itself passing containment, the unrunnable method's own remedy, the bundle's destination on TypeScript, and a blanket "never regenerate" where the old text scoped it to its rows. None was a placement error, so no smoke session could see it; the two review rounds found them all by comparing the new sentence with the old. **The correction for inputs and design**: a classification row quotes every condition, qualifier and remedy of the sentence it classifies, and the rewrite is read against those quotes before the first review, not after.
- **No moved caveat was missed in a smoke session** ([`smoke-integrate.md`](smoke-integrate.md)): each branch reference was read on its branch and none on the main path.
- **Box B's shape needs no correction, but its budget does.** The shape fitted: a main path of short imperatives, a stop table, references pointed at their conditions. The margin did not: after the review's fixes the Claude render sits 52 characters under the ceiling, so every bug waiting on this phase lands in a reference or pays for itself in `SKILL.md`. For inputs and design the target is to finish the rewrite well under the ceiling — about 12,000 characters — so the review's restorations have room.

## Phase 3 — `pipelex-inputs` · `L-260923-e7a655`

- [x] `classification-inputs.md`.
- [x] The main path: the three target forms, the choice of strategy, the template call and its verdicts, the path rule of `inputs.json`, the core of preparation (what leaves the machine, absolute paths, the exact file and never a derived one, `inputs.prepared.json` written, `inputs.json` never rewritten), the conditions for offering a run, the stop table.
- [x] References: the synthetic and user-data detail, the size-limit failure, the stale-workshop refusal met only by an address, the worked examples.
- [x] Guards registered, smoke sessions run: the template strategy, a user's PDF prepared and offered, a synthetic file through `pipelex-synthetic-inputs`, a published address, and the Codex main path. **The guards are registered** (the `pipelex-inputs` entry of `GUARDS` in `tests/unit/test_skill_guards.py`), and **every smoke scenario passed box H's test**, recorded with its session ids in [`smoke-inputs.md`](smoke-inputs.md); the one expected outcome missed, the run offer on Codex, predates the diet.
- [x] `/rev`. Four rounds at profile 4 (cubic, Codex review and adversarial, code-review). The first, whose adversarial pass approved, restored the no-signal row's interactive question the rewrite had dropped, made `user-data.md`'s report follow the skill's step 5 rather than a section number it clashed with, gave a list input's copied files distinct names, and brought the campaign records up to date. The second restored the instruction to skip preparation when every file value is already remote, which the classification had deleted as a duplicate of a sentence that only described the case, and closed a gap older than the diet: step 5 now deletes a prepared file an earlier pass left, which a skipped, declined or failed preparation would otherwise leave for `/pipelex-run` to run stale or to hand back. The third, at the necessity bar, spared the Template strategy that deletion, since a template pass may have just replaced the values the prepared file alone still holds, and gave a list input's files a directory of their own after the first round's naming rule let a list item alias another input's copy. What it found older than the diet is deferred below. The fourth, at the freeze bar, fixed one thing: a worked example still showed a list flat under `inputs/`, and the index given to a duplicate name could itself be a real file's name, so a list's files are now named by their position.

- **Rendered sizes after the rewrite**: 12,816 characters on Claude, 12,719 on Codex and 12,740 on Vibe after the review's fixes, all under the 13,000 ceiling; the Claude render was 41,480 characters before. That is above the phase's target of about 12,000, and the difference is checkpoint 2's correction applied before the review rather than after it: reading the draft against the classification's quotes restored qualifiers and remedies a shorter draft had dropped — the `mthds_list_methods` fallback, the scope of a mid-run switch to interactive mode, "with no further context" on the no-signal row, "the same prohibition applies after an upload failure", "no `explicit` flag" on the prepare call, the webapp editor and "source the user does not have" on an invalid template, what the one-line prepare report says. What the rest of the render holds is the guards, the sentences the tests pin, and the fixed blocks: the frontmatter (about 990 characters), the `validate-call` include (about 750) and the requirements include (about 560).
- **The references, each with its entry condition**, all indexed at the foot of `SKILL.md`: `synthetic.md` (step 3, the Synthetic strategy, or Mixed's unfilled inputs, before generating anything), `user-data.md` (step 3, the User data strategy, or Mixed, before matching any file), `published-address.md` (step 1, a `method_ref` target, before the first call; and from the stop table when prepare refuses an address with `input_domain` at `files` or with `config`), `prepare-errors.md` (from the stop table when prepare returns `input_domain` at `inputs`, `pipe_ref`, `method_id` or `method_ref`, before replying or retrying). The worked examples went to the strategy they illustrate; the size-limit failure is the first section of `prepare-errors.md`, whose pointer line in the stop table says the limit is terminal; the stale-workshop refusal is `published-address.md`'s, beside the other address-only refusal and the rest of what makes an address unlike an id. `tests/unit/test_pipelex_inputs_skill.py` holds the shape: each reference pointed at on the line where its branch is taken, static, opening with its entry condition, one level deep, shipped byte for byte, and the render under the ceiling while the check only reports.
- **What the smoke sessions showed** ([`smoke-inputs.md`](smoke-inputs.md)): each branch reference was read on its branch before acting — `user-data.md` before any file was matched, `synthetic.md` before anything was generated, `published-address.md` before the first call on an address, whose default `<output_dir>` the session took from it — and none on the template's main path, so no moved caveat was missed and no template changed. Every session that prepared announced the upload first, sent the path absolute, wrote `inputs.prepared.json` beside an `inputs.json` left as saved, and gitignored the prepared file; no session called a run tool, which a session-only hook would have refused. Claude offered the run in every scenario that reached it; Codex never did when the prompt told it to stop short of a run, and a control run of the pre-diet skill did the same, so the gap predates the diet and, by box G, a fix lands after the phase (`L-260923-530eb0`). The skill's quoted mock URL was stale and is corrected here; that the workshop refuses a `{path}` outside its launch directory, which the shared `validate-call` include does not say, is `L-260923-019e50`. For the eval suite `L-260921-479a8e`: an unattended Codex run must pre-approve `mthds_prepare_inputs`, and `prepare-errors.md` was reached by no scenario.
- **The behaviour changes** are listed at the top of [`classification-inputs.md`](classification-inputs.md): preparation no longer said anywhere to rewrite `inputs.json` (`L-260922-e01c73`'s sentences, resolved for the prepare step's version, and the haiku example deleted with its wrong directory), the closing summary names `inputs.prepared.json` only where prepare wrote one, and the worked PDF example sends its path absolute. PR #49, `L-260922-e01c73`'s own fix, touches the same sentences in the old shape; whichever lands second resolves the conflict towards the new shape, which says the same thing.

## Phase 4 — `pipelex-design` · `L-260923-9d9b59`

- [x] `classification-design.md`.
- [x] The main path: contract capture, the choice of mode, direct construction, the validation verdicts, the runnable gate, delivery.
- [x] References: stepwise construction and re-entry; the catalog-id resolution through the include design, edit and organize share — which design now reads from a shared reference rendered per target, `skills/shared/catalog-id.md`, whose template includes that block whole (see the rulings below).
- [x] The question of design section 6, answered and recorded: whether `references/writing-mthds.md`, read before every write, is split by pipe type. Not a commitment. **Answered: no split.** The ceiling protects `SKILL.md`, which a compaction re-attaches; a reference read into the conversation is never re-attached, so a split changes nothing a compaction keeps. Choosing the operators needs the whole catalogue, so per-type files would be read after the choice they inform, or the model would choose among the types it happened to read — silently worse design and no stop. References are one level deep, so `SKILL.md` would pay for a pointer per file to save the reference's characters. And every write needs the file's shared sections anyway, so a split saves only the pipe types a design does not use, a token cost per design that belongs to the context diet. Revisit when the behavioural evals (`L-260921-479a8e`) show the file crowding a session. Recorded in `docs/decisions.md`, "`pipelex-design`'s rationale, moved out of the skill".
- [x] Guards registered, smoke sessions run: a direct design, a stepwise design, a re-entry, a re-entry by catalog id, and the Codex main path. **The guards are registered** (the `pipelex-design` entry of `GUARDS` in `tests/unit/test_skill_guards.py`, the bridge's two guards included), and **every smoke scenario passed box H's test**, recorded with its session ids in [`smoke-design.md`](smoke-design.md); the search for a linked directory was composed beside the catalog-id reference rather than after it, and the first stepwise run met the hosted API's cap on files per validation, a limit the stepwise rule reaches independently of the diet.
- [x] `/rev`. Four rounds at profile 4 (cubic, Codex review and adversarial, code-review). The first restored what the rewrite had dropped and the read against the quotes had missed: the runnable gate holds for a completed method only, a re-entry restoring at least its baseline verdict; the two stepwise signals that are not complements of direct win even where direct holds; the re-entry line points at its references, the authoring reference included, before the baseline repair; and an early-stopped scaffold goes back to the delivery notices. It also made the catalog-id reference say where to continue. The second made that continuation conditional on a catalog id resolving, since a published address ends the calling skill's work, and had `re-entry.md` say in words to read the stepwise reference before building a signature-driven scaffold. The third, at the necessity bar, routed a request to finish an existing scaffold through the re-entry to the stepwise loop; the fourth, at the freeze bar, found that route could rewrite the scaffold's root over the user's concrete definitions, and withdrew it, so finishing a scaffold is deferred below with what the review learned about it.

- **Rendered sizes after the rewrite**: 12,899 characters on Claude, 12,720 on Codex and 12,727 on Vibe as drafted, and 12,893, 12,714 and 12,721 after the review's fixes, all under the 13,000 ceiling; the Claude render was 32,645 characters before. The draft's margin on Claude was 101 characters, so the review's restorations paid for themselves: the organize item of step 6 lost a sentence that restated its own rule, and the hand-off to `/pipelex-integrate` stopped describing what that skill generates. That is above both the phase's target of about 12,000 and the orchestrating session's revised aim of about 12,650, for a reason inputs did not have: once the moves below were made, the shared blocks design must carry still render to about 4,900 characters (frontmatter 1,094, saved-copy notice 1,098, stale-types notice 798, `validate-call` 702, requirements 642, the `PipeFunc` warning twice 402, project-root markers 140), against about 2,300 in inputs. Before those moves they were about 7,100, and no wording of the main path got under the ceiling. The read against the classification's quotes then restored qualifiers worth about 215 characters, listed at the end of [`classification-design.md`](classification-design.md).
- **Two rulings of the orchestrating session, 2026-09-23.** First, the catalog-id bridge (about 1,930 characters rendered) became the shared reference `templates/skills/shared/catalog-id.md.j2`, rendered per target, on four conditions, each met: the bridge's guards stay in `SKILL.md` at the pointer and are registered; the reference opens with its entry condition and is generic, so edit and organize can point at it unchanged; the Deferred section below carries their switch; and the reason is recorded in the classification, `docs/decisions.md` and here. The saved-copy notice was judged by the same test and stays, because its content is three guards. Second, the formatting-hook include left design as a stop — the hook's block names the syntax error and the harness refuses an edit to a file changed since it was read — its content still living in `docs/hooks.md` and in edit's and organize's include. Moving the bundle-home rules to a reference read on every new design was refused.
- **The references, each with its entry condition**, all indexed at the foot of `SKILL.md`: `stepwise.md` (step 3 chooses stepwise, before writing any file; step 4 meets a placeholder, a guessed contract or an unresolved boundary, before changing any file; a signature-driven re-entry, before building its scaffold), `re-entry.md` (a structural change to an existing method, once the baseline is recorded, before editing any file), and the shared `../shared/catalog-id.md` (a catalog id or a published address as the target, before reading any file). `writing-mthds.md` is unchanged and read before every write. `tests/unit/test_pipelex_design_skill.py` holds the shape: each reference pointed at on the line where its branch is taken, the branch references static, opening with their entry condition and one level deep, all shipped byte for byte, and the render under the ceiling while the check only reports.
- **What the smoke sessions showed** ([`smoke-design.md`](smoke-design.md)): each branch reference was read on its branch before any file was written or edited — `stepwise.md` before the root scaffold, `re-entry.md` before the first edit of a re-entry, the shared `catalog-id.md` at the first tool call on a catalog id — and none on the direct main path, on either harness, so no moved caveat was missed and no template changed. Every re-entry validated its baseline before editing, and the one with a saved `inputs.json` left it as saved and flagged its drift; every catalog-id run either pulled through `/pipelex-catalog` or found the linked directory, made the saved-copy notice and did not save; no session called a run tool or a save, which a session-only hook would have refused. Two observations for checkpoint 3: the catalog-id search was composed in the same turn as the reference read, with `find` rather than the reference's `grep`, because the skill's description already says how an id resolves, though every decision the reference governs still came after it; and `re-entry.md` was read beside or before the baseline validation rather than after its verdict, though always before the first edit. Found on the way, for filing: the stepwise mode's one-file-per-refinement rule, older than the diet, reaches the hosted API's cap on files per validation on a large graph, and the session that met it improvised a regrouping; and a validation's dry run follows one outcome of a `PipeCondition`, so a defect on the other branch passes validation. For the eval suite `L-260921-479a8e`: a read must be counted by what the tool result printed, since zsh drops everything after an `echo =====` separator, and the design main path on Codex needs no approval entry.
- **The behaviour changes** are listed at the top of [`classification-design.md`](classification-design.md): the `runs/` exclusion stated once (phase 1's deferral), the stale-types search and the saved-copy notice made at delivery for whatever the run changed (a contradiction with `pipelex-organize`, resolved for box R of the plugin-skills-gaps design), and the formatting note dropped (the ruling above).

Waiting on this phase: none but `L-260911-3a85ca`, which is ranked urgent and lands whenever it is ready.

### Checkpoint 3

The three most invoked large skills are in the new shape. Record their sizes, what each smoke session showed, and the ceiling check's report.

Written 2026-09-23, after phase 4 landed as PR #57 (`cda0ea3` on `dev`), and carried by phase 5a's branch.

- **Their sizes at `cda0ea3`**, all under the 13,000-character ceiling on every target:

  | Skill | Before, on Claude | Claude | Codex | Vibe |
  |---|---|---|---|---|
  | `pipelex-integrate` | 60,753 | 12,983 | 12,841 | 12,887 |
  | `pipelex-inputs` | 41,480 | 12,816 | 12,719 | 12,740 |
  | `pipelex-design` | 32,645 | 12,893 | 12,714 | 12,721 |

  `pipelex-integrate` grew by 35 characters on Claude after its phase, through `L-260923-e3b8e1` and `L-260923-ed42a7`, both landed in the new shape as one edit each, which is what box G promised of an open bug that waits for its skill's phase. None of the three is near the phase target of about 12,000. The reason is the same each time: the shared blocks a skill must carry, and the qualifiers the read against the quotes restored. So the margin under the ceiling is thin, and a later edit to one of these skills pays for itself or moves a branch to a reference.
- **What the smoke sessions showed** ([`smoke-integrate.md`](smoke-integrate.md), [`smoke-inputs.md`](smoke-inputs.md), [`smoke-design.md`](smoke-design.md)). On Claude Code and on Codex, every branch reference was read on its branch before the action it governs, and none on a main path. No moved caveat was missed and no guard broke, so no template changed for the diet's sake. Two patterns recur, and neither broke anything. A model reads a reference in the same turn as the action it governs, as the catalog-id search showed. And it reads a reference beside the check that decides whether it is needed, as with `re-entry.md` and the baseline validation. What the sessions found older than the diet was filed: Codex never offers the run (`L-260923-530eb0`), the workshop refuses a path outside its launch directory (`L-260923-019e50`), the stepwise mode outgrows the API's cap on files per validation (`L-260923-579324`), and validation follows one branch of a `PipeCondition` (`L-260923-998db9`, `pipelex`'s). The scenarios are written in the shape the eval suite `L-260921-479a8e` will take.
- **The ceiling check's report**, from `make check` on phase 5a's branch after its rewrite. `pipelex-scaffold` is over on every target, at 40,486 characters on Claude after this phase, down from 63,683, and phase 5b takes the method-app path out. `pipelex-catalog`, `pipelex-run`, `pipelex-explain`, `pipelex-organize`, `pipelex-synthetic-inputs` and `pipelex-edit` are over on every target, and phase 6 trims them. Every other skill is under on every target.
- **Nothing is pending a person.** The one decision the checkpoint could raise, whether the thin margins call for a lower target on the remaining skills, is answered by the rule already in force: a skill ends under the ceiling, and the target of about 12,000 is an aim, not a gate.

## Phase 5a — `pipelex-scaffold`: the starters leave, branch B runs through scripts · `L-260923-7febe6`

- [x] `classification-scaffold.md`.
- [x] The starter path is deleted, with `references/starters.md` and its tests: the Python starter, the gallery, the starter's bootstrap and env-file step, the GitHub-template form and the starter half of the fresh-clone shortcut (box E's amendment). Every project that is not a TypeScript web app goes to the ecosystem's initializer and then to `/pipelex-integrate`. The `python_min` and `python_max` floors stay, restated as `pipelex-sdk`'s range: review round 3 found that without them branch B created a project on a Python the SDK refuses.
- [x] Branch B's programs become scripts: the repository test with the pristine commit, and the env-file write, which never prints a key and reports `filled`, `kept` or `empty` and the plane as `production` or `other`. The unit suite executes them directly. They are `skills/pipelex-scaffold/scripts/commit-pristine.sh` and `write-env-file.sh`, executed by `tests/unit/test_pipelex_scaffold_scripts.py`.
- [x] References, each entered on its condition: the version-manager activation, the GitHub form, the per-framework detail in the existing `references/initializers.md`. They are `references/version-managers.md` and `references/github.md`, and `initializers.md` now opens with its entry condition.
- [x] The method-app path is left as it stands; it moves in phase 5b.
- [x] Guards registered, smoke sessions run: a Python CLI, a FastAPI service, a TypeScript library, a directory holding only `.git`, and the Codex main path. The sessions, with one more for a user who names a starter, are recorded in [`smoke-scaffold.md`](smoke-scaffold.md): every guard held, but `version-managers.md` was read off its branch in the `.git` scenario, which needs a wording fix in the template.
- [x] `/rev`: four rounds at profile 4, the last clean at the freeze bar. Round 4's deferrals are noted on `L-260923-0d9cb6` (the version-manager reference and an index-only `.env`) and on `L-260923-6b46c8` (documentation, fixed in phase 6's first commit).

Landed as pipelex-plugins#58, squash `e5287a5` on `dev`.

- **Rendered sizes after the rewrite**: 40,486 characters on Claude, 40,497 on Codex and 40,511 on Vibe, down from 63,683 on Claude; 41,951, 41,962 and 41,976 once the review rounds' fixes landed. The skill stays over the ceiling until phase 5b moves the method-app path, as box I's order expects.
- **The behaviour changes** are listed at the top of [`classification-scaffold.md`](classification-scaffold.md): the starters are no longer scaffolded from, the template-checkout stop names the method-app family alone, an existing `.gitignore` that does not ignore a `node_modules/` directory present gains that line, and an `.env.example` the initializer wrote keeps its own lines.
- **What the smoke sessions showed** ([`smoke-scaffold.md`](smoke-scaffold.md)): `initializers.md` was read before the initializer in every scenario, on Claude Code and on Codex, and `github.md` in none, since no scenario asked for GitHub. Every session that reached the pristine commit ran `commit-pristine.sh` and `write-env-file.sh` by path and retyped no step of either; each printed `committed:`, and each env verdict — `filled base-url=copied plane=other` with fake credentials, `empty base-url=file plane=production` with none — matches the file on disk. No key was printed or asked for, no env file was read back, no `gh` was run, no SDK dependency or `methods/` directory was added on scaffold's account, and nothing was written into a directory holding more than a lone `.git`. A user who named `pipelex-starter-python` was told the skill no longer starts from the starters and was offered the initializer and the method app. One reference was read off its branch: in both runs of the lone-`.git` scenario, the model saw `node` under `~/.nvm` and read `version-managers.md` although `node` answered, because the report's pointer, shortened by this phase, says "reached `node` through a version manager" where the pre-diet text said "only reachable". Fixed on this branch after the sessions, each with a test: that pointer, which now names a runtime missing from the `PATH`; both scripts' ignore tests, which honoured the user's global excludes file, so a uv project on this machine got no `.env` line of its own; the default `.gitignore`, which was Node's even in a Python project; branch B's commit in a lone-`.git` directory, which now confirms as branch A's does; and `initializers.md`'s pointer into another skill's reference. The last three predate the diet. On Codex, the hand-off wording runs a scaffold prompt that names a method on into `/pipelex-integrate` in the same session. For the eval suite `L-260921-479a8e`: this machine's `~/.zshenv` exports the key into every shell the harnesses start, so the sessions ran with a stand-in `ZDOTDIR`, and a scaffold prompt must say where the project goes, since the shared closing sentence reads as naming the working directory.

Waiting on this phase: `L-260915-4b00fe` and `L-260912-059765`, both about branch B's env file, which its script now owns. And `L-260923-7001f2`, which routes a Python CLI to the family's `cli-python` template (epic `L-260923-71f4a9`) on top of this phase, through the family's initializer rather than a recipe of the skill's own.

## Phase 5b — `pipelex-scaffold`: the method app through the family's own commands · `L-260923-0d9cb6`

Blocked by `L-260922-12f302` (the initializer and the serve target in `pipelex-method-apps`).

- [x] The method-app path becomes the initializer, then the serve target, and the stop table keys on the verdicts they print; the acquisition and dev-server chains leave the skill, and their tests leave with them for the family's own. Commit `a6eae4e` on `feature/Scaffold-method-app-via`; the classification is [`classification-scaffold-method-app.md`](classification-scaffold-method-app.md), and a copy `make create` has not run in is the new `references/uncreated-copy.md`.
- [x] The ceiling check flips from reporting to failing, if phase 6 has already landed. Phase 6 had landed, so `SKILL_CEILING_ENFORCED` is gone and `make check` fails any rendered `SKILL.md` over 13,000 characters.
- [x] Smoke sessions: the method app from a local bundle, from a catalog id, and the Codex main path. Run against a snapshot of `a6eae4e`, with a no-key scenario and a `--no-create` copy added, and recorded in [`smoke-scaffold-method-app.md`](smoke-scaffold-method-app.md). Every scenario passed but Codex under `workspace-write`, whose sandbox denies `ps` (`L-260923-e82e4a`); the four wordings the sessions exposed were fixed in `f13be75`.
- [ ] `/rev`.

- **Rendered sizes**, in characters on Claude, Codex and Vibe: 12,923, 12,936 and 12,950 at `f13be75`; 12,929, 12,942 and 12,956 after review round 1, which cut a clause the branch table already carries to pay for two restored rows; 12,953, 12,966 and 12,980 after round 2.
- **Review round 1**, at profile 4 and bar `open` (cubic, Codex review and adversarial, code-review), reviewed `f13be75`. Fixed: a copy the initializer left inside another repository's work tree, which a later session would have given a nested repository through the hand-made copy's git reading; the plane of an `.env.local` the user wrote after `refused: no-key`, which the report named production; the unstaging of a file rewritten since it was staged, which git refused without `-f`; the `nothing-to-commit` row the rewrite had dropped; the `make serve` verdicts that leave a server the user started still running; the GitHub form for an app with no repository of its own; and the changelog's and decisions' description of the restart warning, which only `node` triggers. Deferred: npm's own sandbox refusal, which the stop table routes as an npm failure (`L-260923-0bee68`). Rejected: the quoting rule, which is about the placeholder and so agrees with every pre-quoted block, and `uv python find`, which only looks for an existing Python.
- **Review round 2**, at profile 4 and bar `defects` (the same reviewers), reviewed `fa0cea1`. Fixed, each introduced by this phase: the report's plane rule now covers only the `.env.local` `make create` wrote, since round 1 gave a file the user wrote its own rule in `uncreated-copy.md` and the two contradicted each other; the plane test defaults an unset `PIPELEX_BASE_URL` to production inside the command, as `make create` does, where it used to print `other`; and the Python interpreter check is scoped to uv again, since dropping "With uv," stopped a poetry, pdm or hatch project on a machine without uv. Deferred: a copy from template 0.2 or 0.3 has no `make serve` (see Deferred). Rejected: that `${CLAUDE_SKILL_DIR}`'s expansion pushes a skill past what a compaction keeps, since the re-attached body excludes the frontmatter and the ceiling is already a conservative stand-in for 5,000 tokens.

## Phase 6 — the rest, and the ceiling bites · `L-260923-6b46c8`

- [x] `pipelex-catalog`, `pipelex-run`, `pipelex-explain` and `pipelex-synthetic-inputs` trimmed under the ceiling by the same rule, each rare branch behind a pointer, and with them any other skill the ceiling calibrated in phase 0 puts over it — `pipelex-organize` first, which sits just under 16,000.
- [x] The ceiling check flips from reporting to failing, if phase 5b has already landed; otherwise 5b flips it. Phase 6 landed first, so phase 5b flipped it.
- [x] `/rev`: four rounds at profile 4, converged with a clean fourth pass; the findings left at the freeze bar are filed and listed under "Deferred".

Landed as pipelex-plugins#59, squash `c5d2a65` on `dev`.

- **Rendered sizes after the rewrite**, in characters on Claude, Codex and Vibe, against the Claude size at this phase's base: `pipelex-catalog` 11,795, 11,523 and 11,530, down from 20,968; `pipelex-run` 10,985, 10,660 and 10,674, down from 20,074; `pipelex-explain` 11,087, 10,886 and 10,893, down from 18,336; `pipelex-synthetic-inputs` 10,818, 10,626 and 10,626, down from 16,010; `pipelex-organize` 10,484, 10,360 and 10,367, down from 16,058; `pipelex-edit` 10,380, 10,201 and 10,208, down from 13,604.
- **Rendered sizes after review**, on Claude, Codex and Vibe: `pipelex-catalog` 11,795, 11,523 and 11,530; `pipelex-run` 11,809, 11,484 and 11,498; `pipelex-explain` 11,195, 10,994 and 11,001; `pipelex-synthetic-inputs` 10,912, 10,720 and 10,720; `pipelex-organize` 10,484, 10,360 and 10,367; `pipelex-edit` 10,380, 10,201 and 10,208.
- **The behaviour changes** are listed at the top of each skill's classification: [`classification-catalog.md`](classification-catalog.md) (the comparison copy is the user's to remove), [`classification-run.md`](classification-run.md) (a run stuck in `RUNNING` is named while it is followed), [`classification-explain.md`](classification-explain.md) (a verdict with no `main_pipe` no longer silences a local bundle's contract), [`classification-synthetic-inputs.md`](classification-synthetic-inputs.md) (a photo of a receipt is recognised as a scanned document at step 1; the integration added the recipe's partial file to step 4's cleanup), [`classification-organize.md`](classification-organize.md) and [`classification-edit.md`](classification-edit.md) (the catalog-id bridge read from the shared reference, the formatting-hook note dropped, and for organize the `runs/` exclusion stated once).
- **Smoke sessions**: [`smoke-phase6.md`](smoke-phase6.md). Every branch reference a session could reach was read on its branch, none on a main path, and no guard broke. Catalog's `conflict.md` and run's `failed-run.md` and `linked-run.md` need a real save or run and were not reached. The Codex harness let one real run through, because Codex's approval policy `never` does not refuse `mthds_run`: filed as `L-260923-f6d5c6`, and the corrected claim is in `smoke-design.md` and `smoke-inputs.md`.
- **Settled here from "Deferred"**: the catalog-id bridge for edit and organize, the formatting-hook note for them (dropped, and the `formatting-hook.md.j2` partial removed with it), and organize's step 1 wording of the `runs/` exclusion.

### Checkpoint 4 — the end of the campaign

Every rendered `SKILL.md` is under the ceiling on every target and `make check` fails one that is not. Record the final sizes against design section 2's table, what the smoke sessions found across the campaign, and the scenarios handed to `L-260921-479a8e`.

## Deferred

- **From phase 5b's review, round 2 (verified):** a copy made from template 0.2.0 or 0.3.0 (by the 0.6.0 skill's clone, or the family's old README recipe) carries `make create` but no `serve` target, which first shipped in 0.4.0. `uncreated-copy.md` runs its `make create`, which is right, and `make serve` then fails with make's own "No rule to make target" line, which the stop table's relay row reports with no URL; nothing is lost, but the user is not told that `make dev` starts such a copy. A line in `uncreated-copy.md` closes it: a copy with no `serve` target predates 0.4.0, so report it created and say `make dev` starts it.
- **From phase 5b's review, round 1 (filed):** `npm create` under Codex's `workspace-write` sandbox fails on npm's own cache before the initializer prints a verdict, and the skill routes a run with no verdict line as an npm failure to relay, so the sandbox hand-off in `uncreated-copy.md` is never reached and a model worked around the cache itself — `L-260923-0bee68`.
- **From phase 6's review, round 4 (filed):** `pipelex-catalog`'s pull tells the user to commit the link file even after a refusal that wrote nothing — `L-260923-bf3f76`.
- **From phase 6's review, round 4 (filed):** `pipelex-run` polls a `degraded` status with no bound, so a long outage of the platform's live view never hands control back — `L-260923-722f1d`.
- **From phase 6's review, round 1:** the per-skill test modules each carry their own copy of the reference-shape helpers — rendering, the reference reader, the static-reference, one-level-deep, byte-for-byte and size-ceiling tests — and the copies have already drifted apart: one entry-condition check wants `"Read this when "` and another `"Read this "`, and one link rule allows `#` anchors. Move the helpers and the shared reference-shape rules into one module that every skill's tests use, and settle each drift on the stricter form.
- **From phase 3's review, round 4 (pre-existing, filed):** `pipelex-inputs`' `references/published-address.md` reads a published package's sandbox refusal as a `config` error, which the workshop now classifies as `input_domain` at `method_ref` — `L-260923-4ce273`.
- **From phase 3's review, round 3 (pre-existing, filed):** `pipelex-inputs` has no strategy for an `inputs.json` that is already filled and only wants preparing, so a hand-back from `/pipelex-run` lands on the no-signal row, whose Template strategy saves placeholders over the filled file without asking — `L-260923-c8f18e`.
- **From phase 2's review, round 2:** nothing checks generically that a skill using `{{ skill_dir }}` includes `shared/skill-dir.md.j2` before its first use; only `pipelex-integrate`'s own test does. Add the check (on the Codex and Vibe renders, the defining sentence precedes the first `<skill-dir>`) in the phase that gives a second skill a script, which box E says is scaffold's (phase 5a).
- **From phase 2's review, round 2 (pre-existing):** the Python list-narrowing snippet in `pipelex-integrate`'s `references/python.md` indexes `main_stuff["items"]` before validating, so a wrong shape raises `KeyError` or `TypeError` rather than a pydantic `ValidationError`; the TypeScript twin validates the envelope first. The cure is a `BaseModel` envelope with `items: list[...]`, then `.model_validate(results.main_stuff).items`, and the pinned snippet in `tests/unit/test_pipelex_integrate_skill.py` with it.
- **From phase 2's review, round 2 (pre-existing):** `pipelex-integrate`'s `references/signature-fallback.md` names the MTHDS reference in words rather than as a link, and SKILL.md links it only from its closing index. A link to `../../shared/mthds-reference.md` from the fallback passes the link check, as `pipelex-design`'s `writing-mthds.md` already does; phase 6 can settle it with the one-level rule's wording in `docs/build-targets.md`, which says both that a reference never sends the model to another and that a shared file may be named by a reference.
- **From phase 2's review, round 2 (pre-existing, filed):** step 10's `cp` and refresh's re-copy overwrite a same-named project script they do not own — L-260923-ed42a7.
- **From phase 4's review, round 4 (filed):** `pipelex-design` has no stated path for a scaffold another skill sends it to finish, nor for a re-entry that ends as a scaffold, which step 6 hands to organize and to the run and integrate skills; and step 5 names no branch for a valid, unrunnable verdict with nothing pending — L-260923-7a803b. A route for finishing was tried in round 3 and withdrawn in round 4: the stepwise reference writes a new root scaffold, which would overwrite an existing one.
- **From phase 4's smoke sessions (filed):** the stepwise mode's one file per refinement outgrows the hosted API's cap on files per validation on a large graph, and the skill does not say what to do — L-260923-579324; and validation checked only one branch of a `PipeCondition`, which is `pipelex`'s — L-260923-998db9.
