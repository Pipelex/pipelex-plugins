---
status: active
item: L-260923-6b46c8
---

# Smoke sessions — phase 6: `pipelex-catalog`, `pipelex-run`, `pipelex-explain`, `pipelex-synthetic-inputs`, `pipelex-organize`, `pipelex-edit`

The smoke sessions that close phase 6 of [`plan.md`](plan.md), under box H of [`design.md`](design.md): a fresh session walks each rewritten skill's main path and each of its branches, and the transcript must show each branch reference read on its branch and none read on the main path, with no guard broken. They ran on 2026-09-23 against the worktree `_pipelex-plugins--trim-smaller-skills` (`feature/Trim-smaller-skills`), starting at `91abb4f`, where `scripts/gen_skill_docs.py --target all --check` reported every generated file fresh. The review round the orchestrating session committed as `42507df` rebuilt the renders while the sessions ran; "How the sessions were driven" says which sessions saw which text, and why it changes no verdict.

**Every scenario that ran passed box H's test, but the Codex harness let one real run through.** On Claude Code, every branch reference that a session could reach was read on its branch and before the action it governs: `python.md`, `unknown-id.md`, run's `published-address.md`, `not-on-disk.md` for a catalog id and for an address, `venv.md`, and the shared `catalog-id.md` from both `pipelex-organize` and `pipelex-edit`. None of them was read on any main path, on Claude Code or on Codex. No session broke a guard of the skill it ran: nothing wrote `inputs.json` or `pipelex-method.json`, explain wrote nothing, and every save and every run a Claude session attempted was an attempt the user's prompt asked for, refused by the harness's hook. The exception is the harness itself. On Codex, the earlier records assumed that `codex exec`'s approval policy `never` refuses `mthds_run`. It does not: Codex 0.153.4 asked approval only for the tools the workshop marks destructive. So the Codex run scenario started, followed and fetched a real run of the small `summarize_pdf` method on api-dev (`run_5fe24881-4cee-46c2-9e28-25d7efa2e52b`, reported cost $0.01522). No save reached the catalog anywhere. The skill did what it says; the failure is the harness's, and it is recorded under "Found on the way" for the eval suite `L-260921-479a8e`.

**Three branches cannot be reached in a smoke session**, because each needs the platform's own answer to a save or a run, and the hook refuses those calls before they leave the machine: catalog's `conflict.md`, and run's `failed-run.md` and `linked-run.md` (scenarios 5 and 8). The link check and the skills' tests hold their pointers. **One template observation, fixed after the sessions**: catalog's pointer to `unknown-id.md` is broader than the reference's own condition, so a pull of an unknown id read a reference written for a save (scenario 4). It did no harm here, and the reference now opens on either tool and gives a pull a paragraph of its own. No template change is needed for box H.

## How the sessions were driven

**Claude Code 2.1.280**, model `claude-opus-5-5`, driven as [`smoke-design.md`](smoke-design.md) and [`smoke-scaffold.md`](smoke-scaffold.md) record: headless with `claude -p` from each scratch project, `--plugin-dir <worktree>/pipelex`, session-only settings disabling the published `pipelex@pipelex-plugins` plugin and the workspace-internal plugins, `--permission-mode bypassPermissions`, `--output-format stream-json --verbose`, and stdin closed. Every init message lists one `pipelex` plugin, `pipelex@inline` 0.7.0 at `/Users/lchoquel/repos/Pipelex/_pipelex-plugins--trim-smaller-skills/pipelex`, and its MCP server `plugin:pipelex:pipelex` as connected. Every session's own transcript under `~/.claude/projects/` carries `Base directory for this skill:` in that worktree for the skill it was given, and scenario 17's carries `pipelex-catalog`'s as well. The workshop was the plugin's own launcher, `npx -y @pipelex/mcp@latest` (0.17.0 on the day), against api-dev (`pipelex-hosted` 0.20.0 by `/v1/version`).

**The session-only hook** is phase 4's `PreToolUse` hook on `mcp__.*`, now logging per session. It logs the name of every MCP tool a session calls and denies, logging it as an attempt, any tool whose name ends in `__mthds_run`, `__mthds_run_status`, `__mthds_run_results` or `__mthds_save_method`. The tools stay in each session's tool list. Every logged name is the plugin workshop's (`mcp__plugin_pipelex_pipelex__…`). The denials are exactly the four the prompts asked for: `mthds_save_method` in scenarios 1 and 3, and `mthds_run` in scenarios 6 and 7.

**What differed from the earlier records.**

- **The claude.ai connectors were switched off.** Since phase 4, this account's headless sessions also load its claude.ai connectors. One of them, "Pipelex Dev", is a hosted Pipelex console that exposes the same tool names as the workshop. A probe session (`045e0d16-c4ca-4a1c-9db6-38f753b74803`) listed it as connected. Every session here ran with `ENABLE_CLAUDEAI_MCP_SERVERS=false`, and its init message lists only `plugin:pipelex:pipelex` and `plugin:context7:context7`, the same two servers phase 4's sessions had.
- **The sessions' shells carry no key.** The Claude Code and Codex processes kept the real key and base URL, so the workshop, which they spawn directly and not through zsh, reached api-dev. `ZDOTDIR` pointed at a stand-in whose `.zshenv` unsets both variables, so every zsh the session's shell tool started carried neither, and this machine's `~/.zshenv` did not export them again. The earlier MCP-backed phases left the key in the shells. The probe session ran a command that prints only whether each variable is set. It printed `key=unset base=unset`, and in the same session the workshop listed the catalog and validated the three hand-made base bundles. The plugin's validation hook ran its format stage (scenario 16). Whether its validate stage saw the key cannot be told from these transcripts, since no session met a hook block.
- **The venv branch ran on a machine without `uv`.** Scenario 13 ran with neither variable set, a `PATH` of `node`, `npx` and `npm` links, `/opt/homebrew/bin` and the system directories, and `XDG_CACHE_HOME` in the scratch directory, so the venv the reference builds landed there and not under the user's home. A probe (`549723c0-6430-45da-a5a6-869de8577c22`) printed `uv=no python3=/opt/homebrew/bin/python3` (3.14.3). `~/.cache/pipelex-plugins` does not exist after the run.
- **The worktree moved during the sessions.** The first batch (scenarios 1, 2, 3, 4 and 6) ran from 12:03:00Z to 12:03:43Z against `91abb4f`'s render. At 12:04:55Z the orchestrating session rebuilt the renders for its first review round, which it committed at 12:06:08Z as `42507df`. Every later session ran against that render, including the Codex cache, which was installed after it. Across the six skills the rendered difference is two sentences. `pipelex-run`'s step 6 gains *"A status marked `degraded` is the last-known one, not a fresh reading: keep polling on its hint, and never call the run stuck or failed from it."*, and `pipelex-organize`'s step 4 writes "Step 1" with a capital letter. No reference and no shared file changed. The only later session on `pipelex-run` on Claude Code, scenario 7, stopped at the denied `mthds_run` before step 6. The Codex run followed a run whose status was never marked `degraded`.

**Codex CLI 0.153.4**, model `gpt-6-astra` at medium reasoning effort, in an isolated `CODEX_HOME` as the earlier phases did it: only `auth.json` copied in, the marketplace added from the worktree root, and the plugin installed with `codex plugin add pipelex@pipelex-plugins`. `diff -r` over the whole plugin found the cache copy identical to the `pipelex-codex/` render. Each run was `codex exec -m gpt-6-astra --json -s workspace-write -C <project> "<prompt>"`, with `ZDOTDIR` as above. The synthetic-inputs run added `-c sandbox_workspace_write.network_access=true`, since a cold `uv` cache needs the network once. No MCP tool had an approval entry. Every rollout carries its skill as `<skill><name>pipelex:<skill></name>` with its path in the cache copy (`…/codex-home/plugins/cache/pipelex-plugins/pipelex/0.7.0/skills/<skill>/SKILL.md`), and records `approval_policy: never`. The user's own `~/.codex` was left untouched: the hash of its `config.toml`, its timestamps and a hash over its `pipelex-plugins` plugin cache are the same after the runs as before them.

**What counts as a read**, as in the earlier phases: a `Read` naming the reference, or a shell command that printed it, counted from what the tool result printed. Each reference was recognised by its own first heading, matched as a whole line in the result. Run's `published-address.md` and inputs' share their heading, so scenario 7's read is attributed by the path its command named. On Codex, reads were counted from the rollout, which is what the model saw, not from the `--json` stream. No session's read was cut short by zsh's `=` expansion this time, because the models quoted their separators (`echo "=== $f"`).

**The base projects** are scratch git repositories outside every repository, each with a committed baseline, so no workspace `CLAUDE.md` or `AGENTS.md` loads and `git status` after a session shows exactly what the session did.

- *Empty*: phase 4's, a `.gitignore` and a README.
- *Summarize*: phase 4's re-entry project, `methods/summarize-pdf/main.mthds` (`PipeSequence` of a `PipeExtract` and a `PipeLLM`, main pipe `summarize_pdf`), with an `inputs.json` naming a local copy of a 979-byte report. No link.
- *Run*: phase 3's prepared project, the same bundle with `inputs.json` and a gitignored `inputs.prepared.json` holding a `pipelex-storage://` reference, the prepared file dated after `inputs.json` and the PDF.
- *Address*: phase 3's scenario 4 project, `documents/inputs.json` and `documents/inputs.prepared.json` for `github.com/Pipelex/methods/documents@v0.1.0`, with no local bundle.
- *Explain*: phase 4's organized `support_email_triage` bundle, four files and twelve pipes (a `PipeBatch` over a sequence holding a `PipeCondition` over two sub-sequences).
- *Organize*: the same bundle split into a construction-shaped layout, one `.mthds` file per pipe (twelve files), the header, the concepts and the main pipe in `main.mthds`.
- *Linked split*: phase 4's linked project, the catalog method `joke` (`mt_fd6c9163-93af-4242-80c3-bfe0ed300a61`) at `flows/joke-judge/` with its committed link file, its `main.mthds` split into one file per pipe.
- *PipeFunc*: `methods/word_stats/`, a `PipeSequence` of a `PipeLLM` summary and a `PipeFunc` (`function_name = "count_summary_words"`), beside four Python files: `word_stats_funcs.py`, which registers the function and imports from `text_utils.py`; `text_utils.py`; a `test_word_stats.py`; and a `scratch_experiment.py` that nothing imports.

The probe session validated the three hand-made bundles through the workshop: each `is_valid: true`, `is_runnable: true`, no pending signature.

**The prompts** end with phase 4's sentences, *"The project is the current working directory. This is an unattended session: nobody can answer questions, so work automatically and take the defaults."* Every prompt that asks for neither a save nor a run adds *"Do not start a run of the method."* The prompts that ask for a save or a run do not, because the refused call is where their main path ends.

**Checking the guards.** After the runs, every stream transcript, persisted Claude transcript and Codex rollout (47 files) was searched for the key and the base URL through the shell variables, printing only counts: neither appears anywhere, and no project file holds the key. `inputs.json` and `inputs.prepared.json` in the run projects hash the same after the sessions as in their bases. `pipelex-method.json` appears in a diff only where `mthds_get_method`'s write arm wrote it (scenarios 2 and 17). No `Write`, `Edit` or shell command of a model names it, and scenario 15's committed link file has no diff. The transcripts and projects were kept in the orchestrating session's scratch directory (`/private/tmp/claude-501/…/scratchpad/p6-smoke/`), which is temporary, so the session ids below are the durable handles.

**The scenario shape** is phase 2's: setup, prompt, expected references and expected outcome, followed by the runs.

## Scenario 1 — `pipelex-catalog`, a save (main path, create)

- **Setup.** The summarize project, with no link file.
- **Prompt**, by the slash command: `/pipelex:pipelex-catalog Save the method in methods/summarize-pdf/ to Pipelex.` followed by the first two shared sentences.
- **Expected references.** None: not `python.md`, `conflict.md` or `unknown-id.md`.
- **Expected outcome.** The create arm, since there is no link. The name is proposed from the bundle and taken, since the session is unattended. `files` has the root file first, with no `python`, no `link_dir` and no `method_id`. One line before the call. `mthds_save_method` is refused by the hook, and the report says nothing was saved.

**Run** — Claude Code 2.1.280, session `60f93b51-b587-466d-be88-1ead458c449a`. **References read:** none. **MCP tools:** `mthds_save_method`, refused. **Outcome:** one command listed the bundle, printed `main.mthds` and looked for a link file. The announcement came as visible text before the call: *"Creating a new method **Summarize PDF** on Pipelex from `methods/summarize-pdf/`. There's no `pipelex-method.json` here, so this is a create. The bundle has no `PipeFunc`, so no Python is sent."* The call carried `files: [{path: <absolute>/main.mthds}]` and `name: "Summarize PDF"`, and nothing else. After the refusal the report says *"Nothing was created on Pipelex, there is no `mt_…` id, and no `pipelex-method.json` was written."*, and the model did not retry. **Verdict: pass.**

## Scenario 2 — `pipelex-catalog`, a pull (main path)

- **Setup.** The empty project.
- **Prompt**: `/pipelex:pipelex-catalog Pull the saved method mt_fd6c9163-93af-4242-80c3-bfe0ed300a61 onto disk so I can edit it.` followed by the shared sentences.
- **Expected references.** None.
- **Expected outcome.** The written arm with `output_dir: "methods/joke"`, since there is no project marker. The workshop writes the source and the link. The report says to commit the link and hands over to `/pipelex-edit` or `/pipelex-design`, and `/pipelex-run`.

**Run** — session `79f111fb-dc0d-4c81-b594-350ec8f5f4b2`. **References read:** none. **MCP tools:** `mthds_list_methods`, `mthds_get_method`. **Outcome:** the model searched every directory up to `/` for a project marker, found none, resolved the name `joke` from the list, and called `mthds_get_method` with `output_dir: "methods/joke"`. The workshop wrote `main.mthds` and `pipelex-method.json`. The report names the source host and date, why the directory was chosen, *"**Commit `methods/joke/pipelex-method.json`.**"*, and the hand-off to edit, design, catalog and run. **Verdict: pass.**

## Scenario 3 — `pipelex-catalog`, a bundle with a `PipeFunc` (`python.md`)

- **Setup.** The PipeFunc project.
- **Prompt**: `/pipelex:pipelex-catalog Save the method in methods/word_stats/ to Pipelex as a new method named Word Stats.` followed by the first two shared sentences.
- **Expected references.** `python.md`, before any `.py` file is chosen for `python`.
- **Expected outcome.** `python` holds `word_stats_funcs.py`, which registers the function, and `text_utils.py`, which it imports. The test file and the scratch file are left out. The `PipeFunc` warning comes with the announcement. The save is refused by the hook.

**Run** — session `9573e033-ad59-4cb8-9b27-2e4bf22f0d50`. **References read:** `python.md`, by `Read` at the third tool call. The first call had printed the directory's files; the save came after the read. **MCP tools:** `mthds_save_method`, refused. **Outcome:** the call sent `main.mthds` and, as `python`, exactly `word_stats_funcs.py` and `text_utils.py`. The report gives the reference's reasoning: *"`text_utils.py` is sent because `word_stats_funcs.py` imports `tokenize` and `most_frequent` from it. Leaving it out would break the method when it runs."*, and it leaves out `test_word_stats.py` as local tests and `scratch_experiment.py`, which *"describes itself as 'Not used.'"* The announcement and the `PipeFunc` warning were composed in a thinking block before the call, not in visible text. The visible report carries the warning after the refusal, paraphrased: *"Custom Python on the hosted service is experimental: it runs in a sandbox with no network access, and a method that validates can still fail when it runs."* **Verdict: pass on box H's test**, with the announcement recorded under "Found on the way".

## Scenario 4 — `pipelex-catalog`, a pull of an unknown id (`unknown-id.md`)

- **Setup.** The empty project.
- **Prompt**: `/pipelex:pipelex-catalog Pull the saved method mt_00000000-0000-4000-8000-000000000000 onto disk so I can edit it.` followed by the shared sentences.
- **Why this scenario.** The reference opens on a save's `input_domain` error at `method_id`, and a save is refused by the hook. But the stop-table row that points to it, and the closing index, name the error without naming the tool. The probe session showed that `mthds_get_method` answers an unknown id at `method_id` (*"The requested resource does not exist"*, with the hint that no method with this id is visible to the key's organization). So a pull reaches the row, and this is the only way to walk the pointer without a refused call.
- **Expected references.** `unknown-id.md`, before anything is concluded from the error.
- **Expected outcome.** The tool's answer relayed, org-scoping explained, nothing written, and no link removed, since there is none.

**Run** — session `80c17242-0aba-4ec8-968a-5d1295f709af`. **References read:** `unknown-id.md`, printed by `cat` at the fifth tool call. That was after `mthds_list_methods` had returned the organization's eight methods without this id, and before `mthds_get_method`: the model read the reference in anticipation, before the error that is its condition came back. **MCP tools:** `mthds_list_methods`, `mthds_get_method`. **Outcome:** with no name to fold, the model passed the id itself as the directory segment (`output_dir: "methods/mt_00000000-0000-4000-8000-000000000000"`). The call was refused at `method_id` and wrote nothing. The report relays the hint, says *"The error doesn't say which host it checked"*, lists the eight visible methods with their descriptions, and does not pull any of them in its place. It applied none of the reference's save-shaped advice (removing a dead link, creating a new method), which a pull does not call for. **Verdict: pass**, with two observations: the reference was read ahead of its condition, and its pointer's condition is broader than the reference's own (see "Found on the way").

## Scenario 5 — `pipelex-catalog`, a save refused at `expected_updated_at` (`conflict.md`)

**Not reachable in a smoke session.** The branch starts from `mthds_save_method`'s own `input_domain` answer at `expected_updated_at`, which the workshop gives only after reading the stored method. Reaching it means letting a save call through, and a save whose check passes is a deployment. The pointer that guards it is the stop-table row at `templates/skills/pipelex-catalog/SKILL.md.j2:63`, with its index entry at line 75. The eval suite can reach it with a fixture method in a throwaway organization.

## Scenario 6 — `pipelex-run`, a bundle directory (main path)

- **Setup.** The run project.
- **Prompt**: `/pipelex:pipelex-run Run the method in methods/summarize-pdf/ with its prepared inputs.` followed by the first two shared sentences.
- **Expected references.** None: not `published-address.md`, `failed-run.md` or `linked-run.md`.
- **Expected outcome.** `inputs.prepared.json` judged current; `mthds_inputs_template` with `explicit: false`; `mthds_validate` over the bundle, runnable; one line naming the target, the pipe, the inputs and the credit; then `mthds_run`, which the hook refuses, and no run id invented.

**Run** — Claude Code 2.1.280, against `91abb4f`, session `ecdf2dc2-8dbb-442a-b8b4-c296b7cdbbc0`. **References read:** none. **MCP tools:** `mthds_inputs_template`, `mthds_validate`, `mthds_run` (refused). **Outcome:** the model printed the inputs and their modification times and said so: *"The prepared inputs are current: `inputs.json` (08:01) and `inputs/document.pdf` (08:00) are both older than `inputs.prepared.json` (08:02)."* The template call and the validation came back runnable. The `{"url": "pipelex-storage://…"}` shape was read as the prepare rewrite, not as drift. `mthds_run` carried the bundle's `files` and the prepared values verbatim. The line naming the run and its credit was composed in a thinking block, not in visible text. After the refusal: *"No run was started, so there is no run ID, no output to report, and no inference credit was spent. I didn't try again because the block comes from the harness settings."* **Verdict: pass.**

## Scenario 7 — `pipelex-run`, a published address (`published-address.md`)

- **Setup.** The address project.
- **Prompt**: `/pipelex:pipelex-run Run the published method github.com/Pipelex/methods/documents@v0.1.0 on the inputs I prepared for it.` followed by the first two shared sentences.
- **Expected references.** `published-address.md`, before the first MCP call.
- **Expected outcome.** Every call carries `method_ref` alone. The inputs are found in `./documents/`, which only the reference says. The address is tagged, so there is no floating sentence. `mthds_run` with `method_ref` is refused by the hook.

**Run** — session `e4d39d41-1c2b-4bcd-b0c5-a466f1094dd9`. **References read:** run's `published-address.md`, printed by `cat` in the first tool call, with a listing of the project, before any MCP call. **MCP tools:** `mthds_inputs_template`, `mthds_validate` and `mthds_run` (refused), each with `method_ref: "github.com/Pipelex/methods/documents@v0.1.0"` alone. **Outcome:** the model read `documents/inputs.json`, `documents/inputs.prepared.json` and their times, judged the prepared set current, and checked it against the template. The validation came back runnable, main pipe `documents.extract_document_markdown`. The run carried the prepared reference verbatim. The report names the address, the pipe and the verdict, and says no run was started. **Verdict: pass.**

## Scenario 8 — `pipelex-run`, a failed run and a refused linked run

**Not reachable in a smoke session.** `failed-run.md` is read at step 8, once a run has come back failed. That needs a run the harness let start, followed by `mthds_run_status` and `mthds_run_results`, and all three are refused. `linked-run.md` is read when `mthds_run` itself refuses a linked directory's `method_id`, which is an answer only the refused call can give. Their pointers are step 8 at `templates/skills/pipelex-run/SKILL.md.j2:81`, with its index entry at line 104, and the stop-table row at line 97, with its index entry at line 105.

## Scenario 9 — `pipelex-explain`, a bundle directory (main path)

- **Setup.** The explain project.
- **Prompt**: `/pipelex:pipelex-explain Explain what the method in methods/support_email_triage/ does.` followed by the shared sentences.
- **Expected references.** None: not `not-on-disk.md`.
- **Expected outcome.** Every file read before anything is said; one `mthds_validate`; complete or scaffold first; the flow from the root, with one passage per module; the `PipeCompose` steps named as not calling a model; nothing written.

**Run** — session `eecd1d67-b542-4eb2-ac9e-d6f1c322a656`. **References read:** none. **MCP tools:** `mthds_validate`, over the four files by absolute path. **Outcome:** one command printed every `.mthds` file outside `runs/` before the validation. The explanation opens *"**support_email_triage is complete.** The workshop says it is valid and runnable, with nothing pending."*, gives the main pipe's signature from the workshop, and draws the flow as a compact text diagram. It then has one passage per file, and names `compile_triage_digest` as a **`PipeCompose`** that *"doesn't call a model"*. `git status` is clean. **Verdict: pass.**

## Scenario 10 — `pipelex-explain`, a catalog id (`not-on-disk.md`)

- **Setup.** The empty project.
- **Prompt**: `/pipelex:pipelex-explain Explain the saved method mt_fd6c9163-93af-4242-80c3-bfe0ed300a61.` followed by the shared sentences.
- **Expected references.** `not-on-disk.md`, before the first call on the id.
- **Expected outcome.** `mthds_get_method` with the id and no `output_dir`; `mthds_validate` with `method_id`; the source explained as a bundle on disk would be; nothing written.

**Run** — session `116b95d7-55b4-493a-a5b2-483e39222456`. **References read:** `not-on-disk.md`, by `Read` at the first tool call. **MCP tools:** `mthds_get_method` with `method_id` and no `output_dir`, then `mthds_validate` with `method_id`. **Outcome:** a full explanation of the stored source, one file and no stored Python, with its signature and the `PipeCondition`'s two outcomes. It closes *"I didn't write anything to disk and didn't run the method. To change it, first use `/pipelex-catalog` to pull it into a local folder, then use `/pipelex-edit` or `/pipelex-design`."* `git status` is clean. **Verdict: pass.**

## Scenario 11 — `pipelex-explain`, a published address (`not-on-disk.md`)

- **Setup.** The empty project.
- **Prompt**: `/pipelex:pipelex-explain Explain the published method github.com/Pipelex/methods/documents@v0.1.0.` followed by the shared sentences.
- **Expected references.** `not-on-disk.md`, before the first call.
- **Expected outcome.** `mthds_validate` and `mthds_inputs_template` with `method_ref` alone, the template with `explicit: true`; the contract only, with a plain statement that the internals are not readable.

**Run** — session `477ad17a-1702-46e6-915c-91e1b8c38720`. **References read:** `not-on-disk.md`, by `Read` at the first tool call. The shared `native-content-types.md` was searched later for `Document`, which is the skill's own pointer. **MCP tools:** `mthds_validate` and `mthds_inputs_template` with `explicit: true`, both with `method_ref` alone. **Outcome:** the signature and input shape from the workshop, then *"**I couldn't see it.** The workshop gives the interface, not the pipes inside it, so I can't tell you which pipe types the method uses or how many steps it takes."* It says the tag pins the answer, and writes nothing. **Verdict: pass.**

## Scenario 12 — `pipelex-synthetic-inputs`, a PDF with `uv` (main path)

- **Setup.** The empty project, on this machine as it is (`uv` on the `PATH`).
- **Prompt**: `/pipelex:pipelex-synthetic-inputs Make a sample PDF invoice I can use to test an invoice-extraction method, saved as inputs/invoice.pdf.` followed by the first two shared sentences.
- **Expected references.** `pdf.md`, the format's recipe, before any code. Not `venv.md`, `png.md` or `office.md`. The format reference is read on every PDF path; `venv.md` is the branch this phase moved out.
- **Expected outcome.** The `uv` rung; an invoice whose totals add up, with fictional parties; the verify step; a hand-off to `/pipelex-inputs`.

**Run** — session `5ea3fb3d-b4fc-4d5b-b8b2-38d001f93d91`. **References read:** `pdf.md`, printed by `cat` in the first tool call, which also ran `command -v uv`, so before step 2's preflight. That is the main path's own reference read early, not a branch. **MCP tools:** none. **Outcome:** the preflight printed `pdf ready`; the recipe ran through the `uv` runner line with `OUT` set; the model reopened the file, re-rendered once to align two blocks, and reported one page of 2.6 KB with fictional parties and totals that add up (*"subtotal 896.40, VAT 20% 179.28, **total 1,075.68 EUR**"*). The only change to the project is `inputs/invoice.pdf`. **Verdict: pass.**

## Scenario 13 — `pipelex-synthetic-inputs`, no `uv` on the machine (`venv.md`)

- **Setup.** The empty project, run with the no-`uv` environment described above.
- **Prompt**: the prompt of scenario 12.
- **Expected references.** `venv.md`, before anything is created or installed, and `pdf.md`.
- **Expected outcome.** The venv built under `$XDG_CACHE_HOME/pipelex-plugins/synth-venv` by the reference's program; the runner line and the verify command on the absolute interpreter path; the venv path and packages stated.

**Run** — session `4f1460ec-2b5e-4083-84a4-6b9970277ce4`. **References read:** `pdf.md` by `Read` at the second tool call; then `venv.md` by `Read` at the third, after the first had printed no `uv`: *"`uv` is not on `PATH`, so this goes to rung 2. Reading the venv reference first."* **MCP tools:** none. **Outcome:** the next command was the reference's program as written, with one added `print` of the reportlab version. It printed `venv ready: …/p6-smoke/xdg-cache/pipelex-plugins/synth-venv`. The recipe ran with the absolute path as its runner line, not `$VENV`, and the page count was verified through the same interpreter, never a bare `python3`. The report says *"`uv` isn't installed on this machine, so I created a separate Python environment with the PDF and image libraries in your cache folder"* and gives the path. It names the packages only loosely. **Verdict: pass.**

## Scenario 14 — `pipelex-organize`, a construction-shaped bundle (main path)

- **Setup.** The organize project.
- **Prompt**: `/pipelex:pipelex-organize Organize the bundle in methods/support_email_triage/.` followed by the shared sentences.
- **Expected references.** None: not the shared `catalog-id.md`.
- **Expected outcome.** The baseline recorded; the plan in one line; the candidate validated inline; the swap, confirmed on disk; the report; the two notices, which here have nothing to name.

**Run** — against the review render, session `5eca6dfb-496e-4f16-9572-ac2a8800f5e2`. **References read:** none. **MCP tools:** `mthds_validate` four times: the baseline, a candidate call by path that the workshop refused, the candidate inline, and the confirmation. **Outcome:** the baseline was runnable with no pending signature, and the plan was `main.mthds` plus `draft_reply.mthds`. The model composed the candidate with a script into a staging directory outside the project. The workshop refused that path (*"Path resolves outside the server's working directory"*), and the model validated the candidate inline, which the skill prescribes anyway: identical verdict. It then wrote the two files, deleted the eleven others (only `.mthds` files), confirmed on disk and removed its staging directory. The report gives the layout, the preserved verdict, the result of the stale-types search, and that there is no link. **Verdict: pass.**

## Scenario 15 — `pipelex-organize`, a catalog id with a linked directory (the shared `catalog-id.md`)

- **Setup.** The linked-split project.
- **Prompt**: `/pipelex:pipelex-organize Organize the saved method mt_fd6c9163-93af-4242-80c3-bfe0ed300a61.` followed by the shared sentences.
- **Expected references.** The shared `catalog-id.md`, before any file is read.
- **Expected outcome.** The reference's search finds `flows/joke-judge/`, which is used with no pull. The organize path runs there. The saved-copy notice names `joke` and its id and offers `/pipelex-catalog` without calling it. The link is untouched.

**Run** — session `77d9589c-4fdf-4ec1-96e8-f61ffe87d4c7`. **References read:** `catalog-id.md`, printed by `cat` in the first tool call, followed in the same command by a listing of file names. **MCP tools:** `mthds_validate` three times: baseline, candidate inline, confirmation. **Outcome:** the second call was the reference's own search, `grep -rl 'mt_fd6c9163-…' --include=pipelex-method.json .`, which printed `flows/joke-judge/pipelex-method.json`. The model then printed the bundle and said *"Exactly one directory is linked to that method: `flows/joke-judge` (link name "joke"). I'll work there."* It called neither `mthds_list_methods` nor `mthds_get_method`. The five pipe files were folded into `main.mthds` from their original bytes, with the verdict identical at each step. The report ends with the notice: *"**The saved method doesn't have this change.** `flows/joke-judge` is linked to the saved method "joke" (`mt_fd6c9163-93af-4242-80c3-bfe0ed300a61`), and this reorganization exists only on disk. Anything that calls that id keeps running whatever is saved in the catalog. `/pipelex-catalog` can compare the two and update the saved copy if you want it to."* The link file has no diff. **Verdict: pass.** Unlike phase 4's catalog-id runs, the search came after the reference had printed and used the reference's own `grep`.

## Scenario 16 — `pipelex-edit`, a prompt change (main path)

- **Setup.** The summarize project.
- **Prompt**: `/pipelex:pipelex-edit In methods/summarize-pdf/, update the prompt of the write_summary pipe so it asks for a summary written for a busy executive, in at most three factual sentences.` followed by the shared sentences.
- **Expected references.** None of phase 6's: not the shared `catalog-id.md`. The shared MTHDS reference is the skill's own step 4 pointer and may be read.
- **Expected outcome.** The automatic mode; the baseline validated before the edit; the prompt changed with `@pages` kept; the verdict restored; no template refresh, since nothing was renamed; the stale-types search; nothing said about a link, since there is none; `inputs.json` untouched.

**Run** — session `28dafdfc-670c-44a1-b0f0-00278d55bb71`. **References read:** none. **MCP tools:** `mthds_validate` twice. **Outcome:** baseline runnable, one `Edit` of the prompt, re-validation runnable. The stale-types search found nothing and there is no link file. The validation hook reformatted the file after the edit, and the model explained that without the formatting-hook sentence the phase dropped: *"a formatting hook ran after the edit. It removed the column alignment of the `=` signs … Only spacing changed; the content and validation are unaffected."* `inputs.json` is unchanged. **Verdict: pass.**

## Scenario 17 — `pipelex-edit`, a catalog id with no linked directory (the shared `catalog-id.md`)

- **Setup.** The empty project.
- **Prompt**: `/pipelex:pipelex-edit In the saved method mt_fd6c9163-93af-4242-80c3-bfe0ed300a61, change the prompt of the write_joke pipe so the joke is at most two sentences long.` followed by the shared sentences.
- **Expected references.** The shared `catalog-id.md`, before any file is read.
- **Expected outcome.** The search finds nothing. The pull goes to `/pipelex-catalog`, whose `mthds_get_method` writes the source and the link. Then edit's path, and the saved-copy notice, with no save.

**Run** — session `0fbac43c-541b-4bd7-a779-4c88c48dd6db`. **References read:** `catalog-id.md`, printed by `cat` in the first tool call with a listing of file names. **MCP tools:** `mthds_list_methods`, `mthds_get_method` with `output_dir: "methods/joke"`, and `mthds_validate` twice. **Outcome:** the third call was the reference's `grep`, which found nothing: *"No directory here is linked to that method, so I'll hand the pull to `/pipelex-catalog` to bring it to disk first."* The model invoked `pipelex:pipelex-catalog` through the `Skill` tool, and the workshop wrote `main.mthds` and the link. Back on edit's path: baseline runnable, one `Edit` of `write_joke`'s prompt, re-validation runnable. The report has the saved-copy notice: *"Everyone calling that id still gets the version stored in the catalog. Use `/pipelex-catalog` to compare the two and to save this change. Saving is a deployment: every caller of the id gets the new prompt on their next call, so I've left that for you to decide."* It also has the reminder to commit the link. No save was attempted. **Verdict: pass.**

## Scenario 18 — the Codex main paths

- **Setup, prompts, expected references and expected outcomes** are those of scenarios 1, 6, 9, 12, 14 and 16, through Codex against the `pipelex-codex/` render, with each skill invoked as `$pipelex:<skill>` in place of the slash command. The synthetic-inputs run had the sandbox's network. No approval entry was configured, on the assumption, taken from the earlier records, that the run and the save would be refused.

On Codex every session printed its own `SKILL.md` from the cache with `cat` first. That is the skill itself, not a reference.

- **`pipelex-catalog`**, thread `01a0ce2c-185b-7610-8b91-a51e1a68c142`. **References read:** none. The announcement was visible text (*"Creating a new method **Summarize PDF** on Pipelex from `methods/summarize-pdf/`."*). `mthds_save_method`, with the root file by path and the name, was refused by Codex: *"MCP tool call requires approval, but approval policy is never"*. The report says no save was confirmed. **Pass.**
- **`pipelex-run`**, thread `01a0ce2c-181c-74a0-8533-75f7c3c29037`. **References read:** none. `mthds_inputs_template` (with `pipe_ref`) and `mthds_validate` came back runnable. Then, in visible text: *"Running main pipe `pdf_summary.summarize_pdf` from `methods/summarize-pdf/` with its current `inputs.prepared.json`. Validation passed. This run spends inference credit."* **`mthds_run` was not refused.** The run started as `run_5fe24881-4cee-46c2-9e28-25d7efa2e52b`. The model reported the id at once, polled `mthds_run_status` on its two-second hint until `COMPLETED` (12:10:29Z to 12:10:44Z), fetched `mthds_run_results`, and reported the summary with *"Reported cost: $0.01522."* No file was written. **Pass on box H's test**, since the whole main path was walked with no reference read and every guard of the skill held. **The harness failed its rule that no session starts a run**: see "Found on the way".
- **`pipelex-explain`**, thread `01a0ce2c-181f-78b0-a8c8-6ccb0e4dfa16`. **References read:** none. One `mthds_validate` over the four files, then the explanation, complete and runnable first, one passage per file, `PipeCompose` named as not calling a model. Nothing written. **Pass.**
- **`pipelex-synthetic-inputs`**, thread `01a0ce2c-181f-7160-8df9-1ed82e585c25`. **References read:** `pdf.md`, before any code; not `venv.md`. The first preflight failed on the sandbox, not on a missing `uv`: *"error: Failed to initialize cache at `/Users/lchoquel/.cache/uv` … Operation not permitted"*. The model set `UV_CACHE_DIR=/private/tmp/pipelex-synth-uv-cache` and stayed on the `uv` rung, which is right, since `uv` was there. It stated the drafted content before rendering and wrote a verified one-page invoice. **Pass.**
- **`pipelex-organize`**, thread `01a0ce2c-18a2-75a2-8e5e-9df7199e0518`. **References read:** none. Baseline, candidate and confirmation through `mthds_validate`, the swap by `apply_patch`, four files. The report is short and correctly says nothing about generated types or a link, since neither exists. **Pass.**
- **`pipelex-edit`**, thread `01a0ce2c-1811-7830-a6af-c701ea11a6b5`. **References read:** none of phase 6's. The shared `mthds-reference.md` was printed before the edit, which is the skill's own step 4 pointer. Baseline and re-validation runnable, the prompt changed by `apply_patch`. The report does not suggest `/pipelex-inputs`, `/pipelex-run` or `/pipelex-catalog`, as step 7 says, much as phase 4's first Codex run left its hand-off thin. **Pass, with a thin report.**

## Found on the way

None of these is a defect of the diet. The catalog pointer's mismatch was fixed on this branch after the sessions, and the rest was filed or left as recorded below. The first two are for the caller to file; a search of the ledger was not made from this session.

- **Codex does not refuse `mthds_run` under the approval policy `never`, so a Codex harness must refuse it explicitly.** In the published workshop 0.17.0 (`dist/local/main.js`), `mthds_run` is annotated `readOnlyHint: false, destructiveHint: false`, like `mthds_prepare_inputs`. `mthds_save_method`, `mthds_get_method` and `mthds_codegen` carry `destructiveHint: true`. Codex 0.153.4 asked approval for the destructive ones, and under the policy `never` refused them. It ran `mthds_run` without asking. [`smoke-design.md`](smoke-design.md) (line 20) and [`smoke-inputs.md`](smoke-inputs.md) (line 25) say a run call would have been refused there, but no earlier Codex session called `mthds_run`, so the claim was never tested. For the same reason, the approval entry those phases configured for `mthds_prepare_inputs` may never have been needed. The cost here was one real run of `summarize_pdf` on a 979-byte report, `run_5fe24881-4cee-46c2-9e28-25d7efa2e52b` on api-dev, reported at $0.01522. For the eval suite `L-260921-479a8e`: a Codex scenario that can reach a run must remove or refuse the run family itself, through the isolated configuration or a Codex hook, and must never rely on the approval policy. Separately, whether a paid call should be annotated so that hosts ask before making it is a question for `pipelex-mcp`, whose tool annotations decide what Codex does unasked. Filed as `L-260923-f6d5c6`, with the harness facts noted on `L-260921-479a8e`.
- **The claude.ai connectors now reach headless sessions**, and one of them is a hosted Pipelex console exposing `mthds_validate`, `mthds_run` and the rest under `mcp__claude_ai_Pipelex_Dev__*`. The guard's suffix match would have refused its run tools. But a session could validate or list through the console instead of the workshop the skill names, and then a scenario would not test the plugin. `ENABLE_CLAUDEAI_MCP_SERVERS=false` restores phase 4's conditions, and the eval suite needs it.
- **Catalog's pointer to `unknown-id.md` is broader than the reference** (scenario 4). The stop-table row at `templates/skills/pipelex-catalog/SKILL.md.j2:64` and the index entry at line 76 name *"`input_domain` at `method_id`"* with no tool. The reference opens *"Read this when `mthds_save_method` answers an `input_domain` error at `method_id`"* (`skills/pipelex-catalog/references/unknown-id.md:3`) and speaks of a save throughout. A pull of an unknown id meets the row and reads a reference whose advice does not apply to it. The model applied none of it, so no harm came of it. Either scoping the row and the index to a save, or giving the reference one sentence for a pull (relay the not-found answer; there is nothing on disk to remove), would make pointer and reference agree. The pre-diet stop row was unscoped too (`git show 44e7fde:pipelex/skills/pipelex-catalog/SKILL.md`, line 161). There, the paragraph on the error sat in the Save section, where a model would not meet it on a pull. Fixed on this branch the second way: the reference's condition names both tools, and a closing paragraph says a pull has no link to judge, relays the answer, and offers the search by name.
- **The line said before a save or a run was often composed in thinking.** In scenarios 3, 6 and 7 the one line that catalog's step 4 and run's step 4 ask for before the call was in a thinking block, not in visible text, and in scenario 3 the `PipeFunc` warning that goes with it. The visible report carried both only after the refusal. Scenario 1 said it visibly, and so did every Codex run. Phases 3 and 4 saw narration land in thinking the same way, so this is the model's habit and not a moved caveat. But these lines tell the user what is about to cost credit or change a deployment, and in a headless stream the user sees only text. The eval suite must decide whether a thinking block counts, and the run and catalog skills may want the line to be the reply's text.
- **A reference read ahead of its condition.** Scenario 4 read `unknown-id.md` after the list suggested a miss and before the error came back. Checkpoint 3 recorded the same pattern: a reference read beside the check that decides whether it is needed. Every decision the reference governs still came after the read.
- **The workshop's path containment, met again** (scenario 14): a candidate staged outside the project was refused by path and validated inline. That is `L-260923-019e50`.
- **A pull of an unknown id has no stated landing directory** (scenario 4). With no catalog name to fold, the model used the id as the directory segment rather than asking. The call was refused and nothing was written, and the segment carries no `/`, `\` or `..`. The pull's step 1 does not say what to do when the id is not in the list, which already answers the question before the call.
- **Codex in the workspace-write sandbox cannot use `uv`'s default cache.** The preflight then prints an error, which is neither the success of rung 1 nor the empty output that sends the model to `venv.md`, and the stop table has no row for it. The model set `UV_CACHE_DIR` under `/private/tmp` on its own, as scaffold's Codex run did, and left `/private/tmp/pipelex-synth-uv-cache` behind. A cold cache also needs the sandbox's network. Both matter to the eval suite.
- **Dropping the formatting-hook sentence did not show** (scenario 16): the model met the hook's reformatting and explained it correctly without the sentence, as phase 4 saw for design.
- **Not exercised by any scenario:** catalog's update arm, meaning a save from a linked directory, with its deployment line; the pull's method-app question and its refusals over a directory that holds a bundle; run's untagged, floating address; run's `Follow a run` entry, whose tools the hook refuses; explain's contract-level fallback when `mthds_get_method` is absent, and a `truncated` read; several directories linked to one method, and a published address given to edit or organize, both in the shared `catalog-id.md`; synthetic-inputs' stops. The link check and the skills' unit tests hold their pointers.
