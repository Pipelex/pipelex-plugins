[plugin]
name = "pipelex"
version = "0.3.0"
description = "Skills and hooks for working with AI methods following the MTHDS standard."
source = "pipelex/"

[vars]
# Inherits all defaults from defaults.toml.
# Override any variable here to change it for this target only.

# [skills]
# include = []  # omitted = all skills
