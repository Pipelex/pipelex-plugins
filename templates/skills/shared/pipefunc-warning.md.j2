{#- Box G of `wip/plugin-skills-gaps/design.md`: `PipeFunc` is supported on the
   hosted plane and warned about, never refused. One sentence, shared word for
   word by every skill that emits, reads, runs or saves a `PipeFunc`, so the
   warning says the same thing wherever the user meets it. The authoring
   reference (`references/writing-mthds.md`) carries the same words.

   Strips its own trailing newline (the closing comment below) so it can be
   included mid-sentence as well as on a line of its own. -#}
**`PipeFunc` is experimental on the hosted plane.** Its Python runs in a sandbox with no network access, and the feature is still in development, so a method that validates can still fail when it runs.
{#- -#}
