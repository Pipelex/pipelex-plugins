{#- Where a project starts: the markers `pipelex-design`, `pipelex-integrate`
   and `pipelex-catalog` share, so that the three agree on where a bundle goes
   and where the code that calls it lives. Included mid-sentence, so it strips
   its own trailing newline (the closing comment below). -#}
the nearest directory holding a `package.json`, a `pyproject.toml`, a `setup.py` or a `requirements.txt` at or above the working directory
{#- -#}
