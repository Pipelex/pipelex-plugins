{#- Box H of `wip/plugin-skills-gaps/design.md`: `/pipelex-design`,
   `/pipelex-edit` and `/pipelex-organize` work on files and stay that way, so
   a catalog id reaches them through a directory on disk — the one already
   linked to that method, or the one `/pipelex-catalog` pulls it into.

   One include for the three, because the bridge is the same gesture in all
   three and only the step it hands back to differs; that step is named by
   `catalog_id_bridge_resume`, which every including skill sets.

   Jinja's default `Undefined` compares unequal to everything without raising,
   so an unset or misspelled `catalog_id_bridge_resume` would render as the
   empty string and pass the build, the freshness check and the tests — the
   bridge would ship telling the agent to carry on with nothing named. The
   `{% else %}` branch emits `PIPELEX_BUILD_ERROR` instead, which
   `scripts/check.py` refuses in any generated file. -#}
This skill works on files, so a **catalog id** (`mt_…`) is not a target it can act on directly: it is resolved to a directory on disk first, and everything after that is the ordinary file-based flow.

1. **Look for a directory already linked to that method.** One search over the link files, from the working directory down: `grep -rl '<the mt_… id>' --include=pipelex-method.json .`. Exactly one hit is the directory to work in — say which one, and go to {% if catalog_id_bridge_resume is defined %}{{ catalog_id_bridge_resume }}{% else %}PIPELEX_BUILD_ERROR: catalog_id_bridge_resume must name the step this skill resumes at{% endif %}.
2. **Several hits are the user's choice, never yours.** More than one directory can legitimately hold the same link: `/pipelex-catalog`'s conflict path tells the user to pull a comparison copy into a sibling directory, and that copy carries the same link and is meant for reading, not for editing. Name the directories and ask which one is the work.
3. **No hit — hand the pull to `/pipelex-catalog`**{% if platform == "claude" %}{% else %}; there is no cross-skill invocation on {{ harness_name }}, so open that skill's `SKILL.md` beside this one and follow it{% endif %}. It brings the method's sources to disk and the workshop writes the link beside them; then carry on with that directory. The search only sees the working directory and below, so a bundle linked somewhere else reads as no hit — if the user knows where it is, ask for the path rather than pulling a second copy.

**What the search proves, and what it does not.** It answers where this method lives locally and nothing else. A linked directory can be behind the catalog, ahead of it, or both at once, and the link records no hashes to tell them apart — so do not present the local files as the saved method's current content. `/pipelex-catalog` is what compares the two, and it is also the only way the work done here reaches the saved copy.

**A published address is not a target for this skill.** `github.com/<owner>/<repo>[/<selector>][@<tag>]` names somebody else's published package: nothing of it is on disk, and there is nowhere to write a change back. Say so and point at `/pipelex-explain`, which reads such an address at the level of its contract.
{#- -#}
