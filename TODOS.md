# TODOS — Dual-MCP: bake the local workshop launcher

Tracker for flipping the plugin-declared `pipelex-mcp` server from the baked hosted URL to the **local workshop launcher** (`npx -y @pipelex/mcp@latest`, stdio), per the revised dual-deployment design. Branch: `feature/Dual-MCP`. Use the checkboxes; update the checkpoint notes at each phase boundary so a fresh session picks up cleanly. The previous tracker (Mistral Vibe stable-hooks migration, completed 2026-07-19) is archived at `wip/vibe-stable-hooks-migration.md`.

## Context (read this on cold start)

**What shipped upstream.** `pipelex-mcp` ships two servers from one capability core (see its `SPEC.md`/`CLAUDE.md`/`README.md`): the hosted **Skybridge console** on Alpic (`https://pipelex-mcp-a3c6a115.alpic.live/mcp`) and the npm-distributed **local workshop** (`@pipelex/mcp`, bin `pipelex-mcp`, stdio; resolves `{ path }` items from disk with cwd containment). Both register identical tool names (`mthds_validate`, `mthds_inputs_template`, the `mthds_run` family) under server key `pipelex`. Since `@pipelex/mcp` 0.5.0 the console is **bring-your-own-key**: it holds no server-side `PIPELEX_API_KEY`; callers supply a per-request `plx_sk_` key (`Authorization: Bearer` header or `?api_key=` on the connector URL), and keyless calls get an instructive `config` no-verdict. The workshop authenticates with `PIPELEX_API_KEY` from its process env — the same export this plugin's hook README already documents.

**Why the flip.** Full rationale: `../pipelex-mcp/wip/plugin-reconciliation.md` §8 (revision 2026-07-21) — read it before changing course. Short version: a plugin bakes a *literal* URL into a shared artifact (no env expansion on Claude Desktop — `docs/decisions.md` amendment 2026-07-16), so there is no channel for a per-user key → under BYOK the baked hosted URL produces verdicts for no one. Meanwhile the builder hosts the plugin actually serves (Claude Code, Codex — terminal, node present, shell env inherited) are exactly where the launcher works, and the workshop was always their *intended* server: submitting local files through the hosted console pays the LLM hand-copy penalty, which no auth mechanism fixes. The flip is therefore **definitive, not a BYOK workaround** — console OAuth (due in weeks) changes only connector-connection prose in the README, zero manifests, zero build mechanics.

## Decisions (settled 2026-07-21; fold into `docs/decisions.md` as dated amendments in Phase 3)

- **Every baked plugin MCP declaration is the local workshop launcher; the hosted console is never a plugin declaration.** Consumers who want the console add it as a connector in their host's own UI (`?api_key=` now; sign-in once console OAuth ships). The old "builder opt-in to local" inverts into this consumer pointer.
- **Launcher: `npx -y @pipelex/mcp@latest`.** The plugin pins nothing (the hosted API stays the compatibility anchor). `@latest` is explicit because bare `npx -y @pipelex/mcp` reuses the cached install without checking for updates; `@latest` re-resolves the dist-tag per spawn. Fallback if V-P4 finds session-start latency unacceptable: pin the exact version and bump it per plugin release.
- **Server key stays `pipelex` in every shape** → flattened tool names unchanged (`mcp__plugin_pipelex_pipelex__<tool>` on Claude Code, `mcp__pipelex__<tool>` on Codex) → skills keep their tool references verbatim.
- **The renderer goes command-only.** `mcp_server_url` is retired, replaced by a `[vars.mcp_server]` command/args block. The url shape is *not* carried speculatively ("don't port dead switches") — it returns the day a consumer-facing target actually wants a baked hosted entry, which is a post-OAuth question.
- **Vibe target unchanged:** no manifest declaration; docs point at manual registration of the launcher.
- **The skills' "No API key is needed on your side" line is retired** — it described the key-holding dev-tunnel server. The workshop uses the caller's `PIPELEX_API_KEY`; the skills surface the `config`-class error `hint` when it's missing or rejected, exactly as they already do for an unreachable server.

## Phase 0 — verifications (record answers inline; probe with hand-edited generated manifests before wiring the build)

- [x] **V-P1 Claude command-shape manifest.** VERIFIED 2026-07-21 (headless `claude --plugin-dir` dogfood sessions). The expected shape `{"type": "stdio", "command": "npx", "args": ["-y", "@pipelex/mcp@latest"]}` is accepted as-is and is the pinned Claude shape. Server registers as `plugin:pipelex` and connects at session start; the tool reaches the model as `mcp__plugin_pipelex_pipelex__mthds_validate` (unchanged from the http era); `PIPELEX_API_KEY` from the launching shell reached the spawned server and a `mthds_validate` call on a sample bundle returned a real verdict (`is_valid: true`). Texture note: plugin MCP servers connect asynchronously (~1s spawn), so tools may be absent in the very first instant of a session — harmless interactively, worth remembering in headless tests.
- [x] **V-P2 Codex command shape.** VERIFIED 2026-07-21 (Codex 0.144.5, local marketplace + `codex plugin add`, live `codex exec` round-trips). Bare `{"command": "npx", "args": ["-y", "@pipelex/mcp@latest"]}` is accepted (no `type` key needed). **Surprise that changed the shape: Codex does NOT inherit the shell env into plugin-spawned stdio MCP servers** — it spawns them with a minimal whitelist (HOME, PATH, USER, SHELL, TMPDIR, LANG + a few), verified with an env-dump probe server; `shell_environment_policy.inherit=all` does not affect MCP spawns. The fix is Codex's `env_vars` field (list of variable *names* forwarded from the parent env; `codex-rs/rmcp-client/src/utils.rs` `create_env_for_mcp_server`), accepted snake_case in the plugin manifest's `mcpServers`. **Pinned Codex shape: `{"command": "npx", "args": ["-y", "@pipelex/mcp@latest"], "env_vars": ["PIPELEX_API_KEY", "PIPELEX_BASE_URL"]}`.** `PIPELEX_BASE_URL` is included because forwarding only the key broke dev-keyed setups (a dev `plx_sk_` key against the default prod base URL gets `Forbidden`); forwarding both restores parity with Claude's full-env inheritance, and an unset variable simply isn't forwarded. With that shape: `codex mcp list` shows the server with `PIPELEX_API_KEY=*****`, a live call round-tripped (`mcp__pipelex__mthds_validate`, `is_valid: true`), and a same-named config-tier `mcp_servers.pipelex` entry (tested via `-c`) fully replaces the plugin entry — the documented dev override holds. Second finding: the workshop's cwd containment means `{path}` items outside the session cwd are rejected — fine for real use (bundles live in the project), worth knowing when testing.
- [ ] **V-P3 Claude Desktop failure UX.** DEFERRED 2026-07-21 — needs the Desktop app; user offered to assist later. Not a blocker (worst case is a noisy failure the docs explain, against a baseline where the hosted URL couldn't produce verdicts either). Remains open for the README-caveat wording (see Open questions).
- [x] **V-P4 `@latest` spawn latency.** VERIFIED 2026-07-21, decision **confirmed: keep `@latest`**. Warm spawn ~0.8–1.3s to a completed MCP initialize (measured over many runs); first-ever spawn ~11s (one-time npx cache population). Registry-down behavior (simulated via unreachable `npm_config_registry`): the spawn hangs in npm's retry backoff and the server never comes up — but a **pinned exact version behaves identically** (npx contacts the registry even for cached exact specs on npm 11/node 24), so pinning buys no offline resilience; offline sessions simply have no MCP tools and the skills' stop-posture covers it. The pinned-and-bumped fallback is moot.

**Checkpoint A** (2026-07-21) — Phase 0 done except V-P3 (deferred, non-blocking). Pinned shapes: Claude `{"type": "stdio", "command": "npx", "args": ["-y", "@pipelex/mcp@latest"]}`; Codex the same minus `type`, **plus `"env_vars": ["PIPELEX_API_KEY", "PIPELEX_BASE_URL"]`** (the one surprise: Codex whitelist-filters MCP spawn env, so the renderer must emit `env_vars` on the Codex platform — Phase 1's injection is platform-conditional beyond the original plan). `@latest` confirmed. Env-inheritance: Claude full shell env; Codex only via `env_vars`. Both platforms round-tripped `mthds_validate` live against the published `@pipelex/mcp@0.5.0` with a real key (dev key + dev base URL). The hand-edited probe manifests were left in place in `pipelex/` and `pipelex-codex/` — Phase 1's `make build` overwrites them with identical renderer-emitted content.

## Phase 1 — build mechanics

- [x] `targets/defaults.toml`: `mcp_server_url` deleted; `[vars.mcp_server]` block added (`command`/`args` + `env_vars = ["PIPELEX_API_KEY", "PIPELEX_BASE_URL"]` per V-P2) with the rationale comment pointing at doc 5 §8.
- [x] `scripts/gen_skill_docs.py` (`make_plugin_json`): stdio shape injected per platform (Claude `type: stdio` + command/args; Codex bare command/args + `env_vars`); comment rewritten to the launcher story; skipped when the target defines no `mcp_server` block. Var loading widened: `_coerce_var` preserves TOML tables and lists (`TemplateVarValue`), so `[vars.mcp_server]` survives the defaults/target merge.
- [x] Tests: fixtures updated to the `[vars.mcp_server]` block; Claude/Codex manifest assertions now expect the stdio/command shapes (incl. `env_vars`); the "no block → no `mcpServers` key" test kept.
- [x] `make build` + `make agent-check` + `make agent-test` — all green 2026-07-21.

## Phase 2 — skill templates

- [x] Swept the MCP-backed skill templates (`pipelex-design`, `pipelex-organize`, `pipelex-edit`, `pipelex-inputs`): the "No API key is needed on your side" line is retired; each skill now states the server authenticates with `PIPELEX_API_KEY` from the session env (the hook's variable), and the `config`-class error bullet names missing/rejected key or unreachable API and surfaces the `hint` verbatim.
- [x] Absent-tools STOP wording updated in all four skills: the manifest *spawns* the local workshop (`npx @pipelex/mcp`), absence usually means `node`/`npx` unavailable or the spawn failed; the `/mcp` pointer kept on Claude. A sweep for stale hosted-URL/server-side-auth mentions in templates found nothing else.
- [x] `make build` + `make agent-check` + `make agent-test` — all green 2026-07-21.

**Checkpoint B** (2026-07-21) — manifests bake the launcher on Claude + Codex, skills tell the truth about auth, all checks green. The V-P1/V-P2 surprise that changed the shapes: Codex's whitelist MCP-spawn env → the Codex entry carries `env_vars: ["PIPELEX_API_KEY", "PIPELEX_BASE_URL"]` (see Checkpoint A). Live dogfood round-trips were done in Phase 0 against the published `@pipelex/mcp@0.5.0` with a real key, using manifest content byte-identical to what the renderer now emits: Claude via `claude --plugin-dir` (`mcp__plugin_pipelex_pipelex__mthds_validate` → `is_valid: true`), Codex via `codex plugin add` + `codex exec` (`mcp__pipelex__mthds_validate` → `is_valid: true`; the installed cache copy already carries the final `env_vars` shape).

## Phase 3 — docs

- [x] `README.md`: MCP passages rewritten per host (Claude: automatic workshop spawn + full-env key; Codex: same + `env_vars` forwarding explained + config.toml command-shape override; Vibe: manual launcher registration). Added the "One install, one server — workshop vs console" section with the host→server matrix, the connector consumer pointer (`?api_key=` flagged as the BYOK-era wording that swaps to sign-in at console-OAuth cutover), the Claude Desktop caveat (console connector, not the plugin, is the Desktop path), and the connector-sync warning. Dev-override instructions updated to the command shape.
- [x] `CLAUDE.md`: Key dependency section rewritten (baked launcher, Codex `env_vars`, no more dev-tunnel URL) + template-variables section (`mcp_server` table).
- [x] `docs/decisions.md`: superseded notes on the two 2026-07-14 MCP-declaration sections (what stands vs what flipped) + new "Dual-MCP flip" entry importing the Decisions list with the definitive-vs-contingent split and the verified shapes/latency findings.
- [x] `docs/build-targets.md`: defaults example + prose + variable table row updated to the `[vars.mcp_server]` table.
- [x] `CHANGELOG.md`: unreleased breaking entry written (launcher replaces baked URL; console stays a documented connector path; skills' auth wording updated).

## Phase 4 — close-out

- [x] `wip/pr-1-review-notes.md`: resolve the pre-publication release gate — item 1 (dev-tunnel URL in prod installs) is moot: no URL is baked anywhere anymore; item 2 (endpoint auth posture) is resolved by BYOK (the deployed console holds no funded key — keyless `mthds_run` calls spend nothing) plus workshop-on-caller's-key. Record the resolution in the file.
- [x] Cross-repo companions in `../pipelex-mcp` (separate change there): the README host→server matrix "Claude Code — How to connect" cell gains the marketplace plugin as a connection channel (it spawns the workshop); doc 5 §8 and the wip/README item 6 amendment were written 2026-07-21 alongside this plan. DONE 2026-07-21 — cell edited in `../pipelex-mcp/README.md` (left uncommitted on its `dev` branch; commit it there).
- [x] Decide the release: bump the plugin + marketplace versions, and rule on whether the repo/marketplace goes public now that the pr-1 release gate is closed. DECIDED 2026-07-21: release cut via `/release`; the user made the repo/marketplace **public** the same day.

**Checkpoint C** (2026-07-21) — **Shipped:** the full Dual-MCP flip is implemented, documented, and green (`make check` + `make agent-test` re-verified at close-out): manifests bake the local workshop launcher on Claude (`type: stdio` + command/args) and Codex (command/args + `env_vars: ["PIPELEX_API_KEY", "PIPELEX_BASE_URL"]`), the renderer is command-only (`mcp_server_url` retired), the four MCP-backed skills carry the caller's-key auth story, and README/CLAUDE.md/decisions/build-targets/CHANGELOG all tell the launcher story. The pr-1 pre-publication release gate is **closed** — resolution recorded in `wip/pr-1-review-notes.md` (no baked URL exists anymore; BYOK console holds no funded key). Cross-repo: `../pipelex-mcp/README.md` matrix cell updated (uncommitted there, on its `dev` branch). All of this branch's work is committed on `feature/Dual-MCP` (not pushed — ask before push/PR; PR target: `dev`). **Stayed open:** the release decision (version bumps + go-public ruling — user call, see the box above); V-P3 Claude Desktop failure UX (deferred, non-blocking, feeds the README-caveat wording); the Open questions below (Cursor docs-only lean, Vibe MCP mechanics, OAuth-cutover README swap).

### Session handoff (2026-07-21, session interrupted at the start of Phase 4)

**State: Phases 0–3 complete and green** (`make build` + `make agent-check` + `make agent-test` all pass); Checkpoints A and B written above. **Nothing is committed yet** — the entire flip sits as uncommitted changes on `feature/Dual-MCP` (renderer, `targets/defaults.toml`, tests, four skill templates, README, CLAUDE.md, docs/decisions.md, docs/build-targets.md, CHANGELOG.md, TODOS.md, plus the regenerated `pipelex/` and `pipelex-codex/` outputs). Rules that still apply: commit on this branch only; ask before pushing or opening a PR (target: `dev`).

**Resume here:** Phase 4, first box. `wip/pr-1-review-notes.md` was read but not yet edited — record the gate resolution exactly as the first box above describes (both gate checkboxes in that file resolve: no baked URL exists anymore, and the BYOK console holds no funded key). Then the cross-repo `../pipelex-mcp` README matrix cell (separate change in that repo — do not mix it into this branch), then the release decision (user call), then write Checkpoint C.

**Machine side effects from the Phase 0 probes** (harmless, but know they exist): the Codex plugin `pipelex@pipelex-plugins` is now installed from the local marketplace (was "not installed") with the final `env_vars` manifest in its cache — re-run `make codex-refresh` after any further manifest change; `~/.npm/_npx` has `@pipelex/mcp@0.5.0` cached; the Claude-side probes used session-only `claude --plugin-dir` (no config touched; the installed `pipelex@pipelex-plugins` Claude plugin remains disabled). The user's shell exports a **dev** key pair (`PIPELEX_API_KEY` = dev `plx_sk_…` + `PIPELEX_BASE_URL=https://api-dev.pipelex.com`) — a live `mthds_validate` verdict requires both to reach the server, which is exactly why `env_vars` forwards both on Codex.

## Open questions

- **Cursor**: docs-only `.cursor/mcp.json` snippet or a real target? Carried from doc 5 §6 — unchanged by the flip; leaning docs-only (the launcher command is one line either way).
- **Vibe's MCP registration mechanics** (URL, command, or none) — carried; blocks nothing here, the Vibe target ships no declaration.
- **V-P3's outcome**: how loud must the Claude Desktop caveat be in the README/marketplace listing?
- **OAuth cutover reminder**: when console OAuth ships, swap the README's `?api_key=` consumer-pointer passage for the sign-in wording. Nothing else in this repo changes.

## Carried over from the previous tracker (archived at `wip/vibe-stable-hooks-migration.md`)

- **`pre_tool` / `post_agent` hook opportunities** — revisit-only; nothing needed today.
- **`mthds-plugins` Vibe support** — deferred 2026-07-19, out of scope.
